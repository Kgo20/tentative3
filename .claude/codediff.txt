{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"plan","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Write","tool_input":{"file_path":"C:\\Users\\kgo20\\.claude\\plans\\je-viens-corriger-mon-sunny-candle.md","content":"# Plan : conformité au cahier des charges pour v1/v1.ino\n\n## Contexte\n\n`v1/v1.ino` implémente déjà une liaison bit-bang full-duplex NRZ entre 2 ESP32 (tâches FreeRTOS `taskTX`/`taskRX` épinglées sur des cœurs séparés, trames DEBUT/DATA/FIN/NACK, détection de préambule avec calibration d\u0027horloge, système de logs différés pour ne jamais bloquer pendant le bit-banging). Un bug récent a été corrigé (suppression de `vTaskDelay` dans la boucle RX qui faisait rater le préambule de la trame suivante) — **cette contrainte \"zéro délai dans la boucle RX\" doit être préservée**.\n\nCe qui manque par rapport à l\u0027énoncé (confirmé par exploration du repo — aucun autre fichier pertinent) :\n- CRC16 réel (actuellement `computeCRC16()` retourne toujours 0x0000 et n\u0027est jamais appelé)\n- Numérotation de séquence + NACK fonctionnel avec retransmission \"go-back-N\"\n- Injection d\u0027erreur aléatoire (bit-flip) pour déclencher un NACK démontrable\n- Simulation d\u0027acquisition de données (5 lignes format capteur, remplaçant le message fixe)\n\nDécisions déjà validées avec l\u0027utilisateur :\n- **Garder l\u0027encodage NRZ actuel** (pas de Manchester), simplification assumée.\n- **Priorité : conformité protocole complète** (CRC16, NACK/go-back-N même si des paquets déjà envoyés doivent être ré-envoyés, injection d\u0027erreur la plus simple possible).\n- **Ne pas toucher au code de timing bit-level** (`detectAndCalibrateOnPreamble`, `receiveBitWithGlobalClock`, `sendBit`, la boucle RX sans délai). Simplifications de confort seulement si elles ne touchent pas à ça — sinon ne rien changer, ~550-650 lignes final est acceptable.\n\n## Implémentation\n\n### 1. CRC16 (CRC-16-CCITT, poly 0x1021, init 0xFFFF)\n- Remplacer le stub `computeCRC16()` (ligne ~341) par une vraie implémentation bit-à-bit.\n- `Frame` : ajouter un champ `bool crcValid` (mémoire seulement, pas sur le fil).\n- `buildFrame()` : assembler `type+seq+len+param+payload` dans un buffer local contigu, calculer et stocker le CRC réel dans `f-\u003ecrc`.\n- `receiveFrame()` : après lecture des octets CRC et END (donc **après** la fin du bit-banging de la trame, aucun risque de timing), recalculer le CRC attendu et fixer `f-\u003ecrcValid`.\n\n### 2. Séquencement + NACK\n- `g_rxExpectedSeq` (débute à 1, reset à chaque trame DEBUT reçue).\n- DATA frames numérotées 1..N (changer `buildFrame(\u0026fData, FRAME_TYPE_DATA, i, ...)` → `i+1`).\n- Dans `taskRX`, branche DATA : si `!crcValid` OU `seqNum != g_rxExpectedSeq` → ignorer le payload et signaler un NACK pour `g_rxExpectedSeq` (ne pas avancer le compteur). Sinon, copier le payload et incrémenter.\n- **Concurrence** : un seul task par carte doit piloter `TX_PIN` pour éviter la corruption du bit-banging — donc `taskRX` ne transmet jamais directement, il pose seulement des flags `volatile`, lus par `taskTX` :\n  - `g_localGapDetected` / `g_localGapTargetSeq` — écart local détecté → taskTX doit émettre un NACK vers l\u0027émetteur distant.\n  - `g_remoteNackReceived` / `g_remoteNackTargetSeq` — NACK reçu de l\u0027autre carte (trame `FRAME_TYPE_NACK`, déjà gérée en réception) → taskTX doit reculer dans son propre envoi.\n  - De simples `volatile bool`/`uint8_t` suffisent (un seul producteur/consommateur chacun, \"dernier gagne\" correct pour du go-back-N) — pas besoin de queue FreeRTOS.\n\n### 3. Retransmission go-back-N\n- Réutiliser le tableau `packets[10][MAX_PAYLOAD]`/`packetLens[10]` déjà présent dans `sendApplicationMessage` (le garder en mémoire pour toute la session suffit à permettre le \"retour en arrière\" — pas de nouvelle structure de stockage).\n- Transformer la boucle `for` d\u0027envoi DATA (lignes ~391-396) en `while (i \u003c totalPackets)` :\n  - Si `g_remoteNackReceived` : consommer le flag, reculer `i = target - 1`, continuer.\n  - Sinon envoyer la trame `i+1`, appliquer l\u0027injection d\u0027erreur, puis vérifier `g_localGapDetected` et transmettre un NACK immédiatement si nécessaire.\n- La trame FIN n\u0027est envoyée qu\u0027une fois la boucle terminée naturellement (aucun NACK non traité en attente), satisfaisant l\u0027exigence \"une fois tous les paquets validés\".\n\n### 4. Injection d\u0027erreur aléatoire\n- Nouvelle fonction `maybeInjectBitError(Frame *f)`, appelée juste avant `sendFrame(\u0026fData)`, **après** que `buildFrame()` ait calculé le CRC (donc le bit-flip devient invisible à l\u0027émetteur et détectable par le CRC du récepteur — simulation réaliste d\u0027une erreur de canal).\n- Probabilité contrôlée par une constante simple (`#define BIT_ERROR_PROBABILITY_PCT 15`), log explicite quand ça se déclenche (`[TX] *** ERREUR INJECTEE ***...`) pour la démo.\n- `randomSeed(esp_random())` ajouté dans `setup()`.\n\n### 5. Simulation d\u0027acquisition de données\n- `acquireDonnees(int index)` retournant les 5 lignes canoniques (format de l\u0027énoncé, \u003c80 caractères chacune).\n- `loadDonneesIntoPackets(...)` remplace `splitMessageIntoPackets` comme source de paquets pour la session envoyée par `taskTX` (5 lignes = 5 trames DATA d\u0027une seule session, cohérent avec `total_paquets=5` → champ `param` de la trame DEBUT).\n- `MESSAGE_TO_SEND`/`splitMessageIntoPackets` restent dans le fichier (inutilisés mais inoffensifs) pour ne pas casser un chemin de test existant, sauf si vous préférez les supprimer.\n\n## Ce qui ne change pas\n`sendBit`, `sendByte`, `receiveBitWithGlobalClock`, `receiveByte`, `detectAndCalibrateOnPreamble`, `logAppend`/`logFlush`, `sendFrame()`, la structure des deux tâches FreeRTOS épinglées, `esp_task_wdt_deinit()`, et surtout **la boucle `while(1)` de `taskRX` sans aucun `vTaskDelay`**.\n\n## Sécurité du timing\nTout le nouveau code (calcul CRC, comparaison de séquence, écriture de flags) s\u0027exécute **après** la fin de la capture bit-à-bit d\u0027une trame dans `receiveFrame()` — dans la même fenêtre où `logAppend` s\u0027exécute déjà aujourd\u0027hui. Aucune insertion dans les boucles de bit-banging elles-mêmes. Le seul effet secondaire : une trame NACK insérée côté TX entre deux DATA introduit un léger espacement supplémentaire, ce qui est sans risque car `detectAndCalibrateOnPreamble` se réarme proprement pour chaque trame (timeout 2s, pas d\u0027hypothèse d\u0027adjacence immédiate).\n\n## Vérification\n1. Compiler/flasher les deux ESP32 (`ESP_ID` 1 et 2).\n2. Observer les logs série : DEBUT (5 paquets), 5 DATA en séquence, FIN — message reconstruit correctement des deux côtés.\n3. Forcer un test de NACK : augmenter temporairement `BIT_ERROR_PROBABILITY_PCT` (ou ajouter un déclenchement forcé sur un paquet donné) pour observer dans les logs : détection CRC invalide → NACK émis → émetteur recule et renvoie à partir du bon paquet → succès final sans FIN prématurée.\n4. Vérifier qu\u0027aucun timeout de préambule n\u0027apparaît dans les logs RX (signe que le timing zéro-délai est resté intact).\n"},"tool_response":{"type":"create","filePath":"C:\\Users\\kgo20\\.claude\\plans\\je-viens-corriger-mon-sunny-candle.md","content":"# Plan : conformité au cahier des charges pour v1/v1.ino\n\n## Contexte\n\n`v1/v1.ino` implémente déjà une liaison bit-bang full-duplex NRZ entre 2 ESP32 (tâches FreeRTOS `taskTX`/`taskRX` épinglées sur des cœurs séparés, trames DEBUT/DATA/FIN/NACK, détection de préambule avec calibration d\u0027horloge, système de logs différés pour ne jamais bloquer pendant le bit-banging). Un bug récent a été corrigé (suppression de `vTaskDelay` dans la boucle RX qui faisait rater le préambule de la trame suivante) — **cette contrainte \"zéro délai dans la boucle RX\" doit être préservée**.\n\nCe qui manque par rapport à l\u0027énoncé (confirmé par exploration du repo — aucun autre fichier pertinent) :\n- CRC16 réel (actuellement `computeCRC16()` retourne toujours 0x0000 et n\u0027est jamais appelé)\n- Numérotation de séquence + NACK fonctionnel avec retransmission \"go-back-N\"\n- Injection d\u0027erreur aléatoire (bit-flip) pour déclencher un NACK démontrable\n- Simulation d\u0027acquisition de données (5 lignes format capteur, remplaçant le message fixe)\n\nDécisions déjà validées avec l\u0027utilisateur :\n- **Garder l\u0027encodage NRZ actuel** (pas de Manchester), simplification assumée.\n- **Priorité : conformité protocole complète** (CRC16, NACK/go-back-N même si des paquets déjà envoyés doivent être ré-envoyés, injection d\u0027erreur la plus simple possible).\n- **Ne pas toucher au code de timing bit-level** (`detectAndCalibrateOnPreamble`, `receiveBitWithGlobalClock`, `sendBit`, la boucle RX sans délai). Simplifications de confort seulement si elles ne touchent pas à ça — sinon ne rien changer, ~550-650 lignes final est acceptable.\n\n## Implémentation\n\n### 1. CRC16 (CRC-16-CCITT, poly 0x1021, init 0xFFFF)\n- Remplacer le stub `computeCRC16()` (ligne ~341) par une vraie implémentation bit-à-bit.\n- `Frame` : ajouter un champ `bool crcValid` (mémoire seulement, pas sur le fil).\n- `buildFrame()` : assembler `type+seq+len+param+payload` dans un buffer local contigu, calculer et stocker le CRC réel dans `f-\u003ecrc`.\n- `receiveFrame()` : après lecture des octets CRC et END (donc **après** la fin du bit-banging de la trame, aucun risque de timing), recalculer le CRC attendu et fixer `f-\u003ecrcValid`.\n\n### 2. Séquencement + NACK\n- `g_rxExpectedSeq` (débute à 1, reset à chaque trame DEBUT reçue).\n- DATA frames numérotées 1..N (changer `buildFrame(\u0026fData, FRAME_TYPE_DATA, i, ...)` → `i+1`).\n- Dans `taskRX`, branche DATA : si `!crcValid` OU `seqNum != g_rxExpectedSeq` → ignorer le payload et signaler un NACK pour `g_rxExpectedSeq` (ne pas avancer le compteur). Sinon, copier le payload et incrémenter.\n- **Concurrence** : un seul task par carte doit piloter `TX_PIN` pour éviter la corruption du bit-banging — donc `taskRX` ne transmet jamais directement, il pose seulement des flags `volatile`, lus par `taskTX` :\n  - `g_localGapDetected` / `g_localGapTargetSeq` — écart local détecté → taskTX doit émettre un NACK vers l\u0027émetteur distant.\n  - `g_remoteNackReceived` / `g_remoteNackTargetSeq` — NACK reçu de l\u0027autre carte (trame `FRAME_TYPE_NACK`, déjà gérée en réception) → taskTX doit reculer dans son propre envoi.\n  - De simples `volatile bool`/`uint8_t` suffisent (un seul producteur/consommateur chacun, \"dernier gagne\" correct pour du go-back-N) — pas besoin de queue FreeRTOS.\n\n### 3. Retransmission go-back-N\n- Réutiliser le tableau `packets[10][MAX_PAYLOAD]`/`packetLens[10]` déjà présent dans `sendApplicationMessage` (le garder en mémoire pour toute la session suffit à permettre le \"retour en arrière\" — pas de nouvelle structure de stockage).\n- Transformer la boucle `for` d\u0027envoi DATA (lignes ~391-396) en `while (i \u003c totalPackets)` :\n  - Si `g_remoteNackReceived` : consommer le flag, reculer `i = target - 1`, continuer.\n  - Sinon envoyer la trame `i+1`, appliquer l\u0027injection d\u0027erreur, puis vérifier `g_localGapDetected` et transmettre un NACK immédiatement si nécessaire.\n- La trame FIN n\u0027est envoyée qu\u0027une fois la boucle terminée naturellement (aucun NACK non traité en attente), satisfaisant l\u0027exigence \"une fois tous les paquets validés\".\n\n### 4. Injection d\u0027erreur aléatoire\n- Nouvelle fonction `maybeInjectBitError(Frame *f)`, appelée juste avant `sendFrame(\u0026fData)`, **après** que `buildFrame()` ait calculé le CRC (donc le bit-flip devient invisible à l\u0027émetteur et détectable par le CRC du récepteur — simulation réaliste d\u0027une erreur de canal).\n- Probabilité contrôlée par une constante simple (`#define BIT_ERROR_PROBABILITY_PCT 15`), log explicite quand ça se déclenche (`[TX] *** ERREUR INJECTEE ***...`) pour la démo.\n- `randomSeed(esp_random())` ajouté dans `setup()`.\n\n### 5. Simulation d\u0027acquisition de données\n- `acquireDonnees(int index)` retournant les 5 lignes canoniques (format de l\u0027énoncé, \u003c80 caractères chacune).\n- `loadDonneesIntoPackets(...)` remplace `splitMessageIntoPackets` comme source de paquets pour la session envoyée par `taskTX` (5 lignes = 5 trames DATA d\u0027une seule session, cohérent avec `total_paquets=5` → champ `param` de la trame DEBUT).\n- `MESSAGE_TO_SEND`/`splitMessageIntoPackets` restent dans le fichier (inutilisés mais inoffensifs) pour ne pas casser un chemin de test existant, sauf si vous préférez les supprimer.\n\n## Ce qui ne change pas\n`sendBit`, `sendByte`, `receiveBitWithGlobalClock`, `receiveByte`, `detectAndCalibrateOnPreamble`, `logAppend`/`logFlush`, `sendFrame()`, la structure des deux tâches FreeRTOS épinglées, `esp_task_wdt_deinit()`, et surtout **la boucle `while(1)` de `taskRX` sans aucun `vTaskDelay`**.\n\n## Sécurité du timing\nTout le nouveau code (calcul CRC, comparaison de séquence, écriture de flags) s\u0027exécute **après** la fin de la capture bit-à-bit d\u0027une trame dans `receiveFrame()` — dans la même fenêtre où `logAppend` s\u0027exécute déjà aujourd\u0027hui. Aucune insertion dans les boucles de bit-banging elles-mêmes. Le seul effet secondaire : une trame NACK insérée côté TX entre deux DATA introduit un léger espacement supplémentaire, ce qui est sans risque car `detectAndCalibrateOnPreamble` se réarme proprement pour chaque trame (timeout 2s, pas d\u0027hypothèse d\u0027adjacence immédiate).\n\n## Vérification\n1. Compiler/flasher les deux ESP32 (`ESP_ID` 1 et 2).\n2. Observer les logs série : DEBUT (5 paquets), 5 DATA en séquence, FIN — message reconstruit correctement des deux côtés.\n3. Forcer un test de NACK : augmenter temporairement `BIT_ERROR_PROBABILITY_PCT` (ou ajouter un déclenchement forcé sur un paquet donné) pour observer dans les logs : détection CRC invalide → NACK émis → émetteur recule et renvoie à partir du bon paquet → succès final sans FIN prématurée.\n4. Vérifier qu\u0027aucun timeout de préambule n\u0027apparaît dans les logs RX (signe que le timing zéro-délai est resté intact).\n","structuredPatch":[],"originalFile":null,"userModified":false},"tool_use_id":"toolu_013JhJK7rJ9wBTDJgmZAyM45","duration_ms":16,"timestamp":"2026-07-02 09:30:55"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc; // TODO: toujours 0x0000 pour l\u0027instant\n} Frame;","new_string":"#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc; // TODO: toujours 0x0000 pour l\u0027instant\n} Frame;","newString":"#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc; // TODO: toujours 0x0000 pour l\u0027instant\n} Frame;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrc = 0x0000; // TODO: CRC réel\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  return true;\n}\n\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)\n  return 0x0000;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nint splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":23,"oldLines":6,"newStart":23,"newLines":8,"lines":[" #define FRAME_TYPE_FIN 0x03"," #define FRAME_TYPE_NACK 0x04"," ","+#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA","+"," // Structure de trame"," typedef struct"," {"]},{"oldStart":31,"oldLines":9,"newStart":33,"newLines":17,"lines":["   uint8_t payloadLen;","   uint8_t param;","   uint8_t payload[MAX_PAYLOAD];","-  uint16_t crc; // TODO: toujours 0x0000 pour l\u0027instant","+  uint16_t crc;","+  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil"," } Frame;"," ","+// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)","+uint8_t g_rxExpectedSeq = 1;","+volatile bool g_localGapDetected = false;","+volatile uint8_t g_localGapTargetSeq = 0;","+volatile bool g_remoteNackReceived = false;","+volatile uint8_t g_remoteNackTargetSeq = 0;","+"," // Message constant dépendant de l\u0027ESP_ID"," #if ESP_ID == 1"," const char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01VgTEeCYbSSHGCkMF59bfUV","duration_ms":26,"timestamp":"2026-07-02 09:32:39"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"void buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrc = 0x0000; // TODO: CRC réel\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n}","new_string":"void buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"void buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrc = 0x0000; // TODO: CRC réel\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n}","newString":"void buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrc = 0x0000; // TODO: CRC réel\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  return true;\n}\n\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)\n  return 0x0000;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nint splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":250,"oldLines":12,"newStart":250,"newLines":20,"lines":["   f-\u003eseqNum = seq;","   f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;","   f-\u003eparam = param;","-  f-\u003ecrc = 0x0000; // TODO: CRC réel","+  f-\u003ecrcValid = true;"," ","   if (payload \u0026\u0026 len \u003e 0)","   {","     memcpy(f-\u003epayload, payload, f-\u003epayloadLen);","   }","+","+  uint8_t crcBuf[4 + MAX_PAYLOAD];","+  crcBuf[0] = f-\u003etype;","+  crcBuf[1] = f-\u003eseqNum;","+  crcBuf[2] = f-\u003epayloadLen;","+  crcBuf[3] = f-\u003eparam;","+  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);","+  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);"," }"," "," void sendFrame(const Frame *f)"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01DqZeVQ4rP2FHjAzNZdRr7P","duration_ms":20,"timestamp":"2026-07-02 09:32:46"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  return true;\n}\n\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)\n  return 0x0000;\n}","new_string":"  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  return true;\n}\n\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)\n  return 0x0000;\n}","newString":"  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  return true;\n}\n\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)\n  return 0x0000;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nint splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":353,"oldLines":13,"newStart":353,"newLines":31,"lines":["     return false;","   }"," ","+  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)","+  uint8_t crcBuf[4 + MAX_PAYLOAD];","+  crcBuf[0] = f-\u003etype;","+  crcBuf[1] = f-\u003eseqNum;","+  crcBuf[2] = f-\u003epayloadLen;","+  crcBuf[3] = f-\u003eparam;","+  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);","+  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);","+","   return true;"," }"," ","+// CRC-16-CCITT (poly 0x1021, init 0xFFFF)"," uint16_t computeCRC16(const uint8_t *data, uint16_t len)"," {","-  // TODO: Implémenter un vrai CRC16 (ex: CRC-16-CCITT)","-  return 0x0000;","+  uint16_t crc = 0xFFFF;","+  for (uint16_t i = 0; i \u003c len; i++)","+  {","+    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;","+    for (int b = 0; b \u003c 8; b++)","+    {","+      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);","+    }","+  }","+  return crc;"," }"," "," // ============================================================================"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01YQWQXvsSMeyGtTtUp8HJEa","duration_ms":21,"timestamp":"2026-07-02 09:32:58"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"int splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","new_string":"// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"int splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","newString":"// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nint splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  if (msg == NULL || packets == NULL || packetLens == NULL)\n    return 0;\n\n  int msgLen = strlen(msg);\n  int packetCount = 0;\n\n  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)\n  {\n    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);\n    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);\n    packetLens[packetCount] = len;\n    packetCount++;\n  }\n\n  return packetCount;\n}\n\nvoid sendApplicationMessage(const char *msg)\n{\n  if (!msg)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);\n\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);\n\n  if (totalPackets == 0)\n  {\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");\n    return;\n  }\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  for (int i = 0; i \u003c totalPackets; i++)\n  {\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);\n    sendFrame(\u0026fData);\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":384,"oldLines":51,"newStart":384,"newLines":107,"lines":[" // LOGIQUE APPLICATIVE"," // ============================================================================"," ","-int splitMessageIntoPackets(const char *msg, uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)","-{","-  if (msg == NULL || packets == NULL || packetLens == NULL)","-    return 0;","+// ============================================================================","+// ACQUISITION DE DONNEES SIMULEE","+// ============================================================================"," ","-  int msgLen = strlen(msg);","-  int packetCount = 0;","+#define TOTAL_PAQUETS 5"," ","-  for (int i = 0; i \u003c msgLen \u0026\u0026 packetCount \u003c maxPackets; i += MAX_PAYLOAD)","+const char *acquireDonnees(int index)","+{","+  static const char *donnees_txt[TOTAL_PAQUETS] = {","+      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",","+      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",","+      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",","+      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",","+      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",","+  };","+  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)","+    return \"\";","+  return donnees_txt[index];","+}","+","+int loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)","+{","+  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;","+  for (int i = 0; i \u003c count; i++)","   {","-    int len = (msgLen - i \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : (msgLen - i);","-    memcpy(packets[packetCount], (uint8_t *)(msg + i), len);","-    packetLens[packetCount] = len;","-    packetCount++;","+    const char *line = acquireDonnees(i);","+    int len = strlen(line);","+    if (len \u003e MAX_PAYLOAD)","+      len = MAX_PAYLOAD;","+    memcpy(packets[i], line, len);","+    packetLens[i] = len;","   }","-","-  return packetCount;","+  return count;"," }"," ","-void sendApplicationMessage(const char *msg)","+// Simule une erreur de canal: inverse un bit aleatoire du payload deja","+// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible","+// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.","+void maybeInjectBitError(Frame *f)"," {","-  if (!msg)","+  if (!f || f-\u003epayloadLen == 0)","     return;"," ","-  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie: \\\"%s\\\"\\n\", TX_PREFIX, msg);","+  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)","+  {","+    int byteIdx = random(0, f-\u003epayloadLen);","+    int bitIdx = random(0, 8);","+    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);","+    logAppend(g_txLogBuf, \u0026g_txLogLen,","+              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",","+              bitIdx, byteIdx, f-\u003eseqNum);","+  }","+}"," ","+// ============================================================================","+// LOGIQUE APPLICATIVE","+// ============================================================================","+","+void sendAcquisitionMessage()","+{","   uint8_t packets[10][MAX_PAYLOAD];","   uint8_t packetLens[10];","-  int totalPackets = splitMessageIntoPackets(msg, packets, packetLens, 10);","+  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);"," ","   if (totalPackets == 0)","-  {","-    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] ERREUR: Message vide\\n\");","     return;","-  }"," ","+  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);","+","   Frame fBegin;","   buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);","   sendFrame(\u0026fBegin);"," ","-  for (int i = 0; i \u003c totalPackets; i++)","+  int i = 0;","+  while (i \u003c totalPackets)","   {","+    if (g_remoteNackReceived)","+    {","+      uint8_t target = g_remoteNackTargetSeq;","+      g_remoteNackReceived = false;","+      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);","+      i = target - 1;","+      continue;","+    }","+","     Frame fData;","-    buildFrame(\u0026fData, FRAME_TYPE_DATA, i, 0, packets[i], packetLens[i]);","+    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);","+    maybeInjectBitError(\u0026fData);","     sendFrame(\u0026fData);","+","+    if (g_localGapDetected)","+    {","+      uint8_t target = g_localGapTargetSeq;","+      g_localGapDetected = false;","+      Frame fNack;","+      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);","+      sendFrame(\u0026fNack);","+      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);","+    }","+","+    i++;","   }"," ","   Frame fEnd;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_0191MZFJemQZPrVM8fn3sPYQ","duration_ms":25,"timestamp":"2026-07-02 09:33:18"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }","new_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }","newString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":517,"oldLines":14,"newStart":517,"newLines":30,"lines":["     if (rxFrame.type == FRAME_TYPE_DEBUT)","     {","       assembledLen = 0;","+      g_rxExpectedSeq = 1;","       logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);","     }","     else if (rxFrame.type == FRAME_TYPE_DATA)","     {","-      if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)","+      if (!rxFrame.crcValid)","       {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",","+                  rxFrame.seqNum, g_rxExpectedSeq);","+        g_localGapTargetSeq = g_rxExpectedSeq;","+        g_localGapDetected = true;","+      }","+      else if (rxFrame.seqNum != g_rxExpectedSeq)","+      {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",","+                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);","+        g_localGapTargetSeq = g_rxExpectedSeq;","+        g_localGapDetected = true;","+      }","+      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)","+      {","         memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);","         assembledLen += rxFrame.payloadLen;","+        g_rxExpectedSeq++;","       }","     }","     else if (rxFrame.type == FRAME_TYPE_FIN)"]},{"oldStart":536,"oldLines":6,"newStart":552,"newLines":8,"lines":["     else if (rxFrame.type == FRAME_TYPE_NACK)","     {","       logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);","+      g_remoteNackTargetSeq = rxFrame.param;","+      g_remoteNackReceived = true;","     }","     else","     {"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01VtYLk6Q2SDAXvx1Y9oPb4x","duration_ms":19,"timestamp":"2026-07-02 09:33:31"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }","new_string":"  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }","newString":"  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendApplicationMessage(MESSAGE_TO_SEND);\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":572,"oldLines":7,"newStart":572,"newLines":7,"lines":[" ","   while (1)","   {","-    sendApplicationMessage(MESSAGE_TO_SEND);","+    sendAcquisitionMessage();","     logFlush(g_txLogBuf, \u0026g_txLogLen);","     vTaskDelay(pdMS_TO_TICKS(5000));","   }"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_011S8DakiTqow9dTfKS2Yezu","duration_ms":24,"timestamp":"2026-07-02 09:33:36"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  Serial.begin(115200);\n  delay(500);","new_string":"  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  Serial.begin(115200);\n  delay(500);","newString":"  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":586,"oldLines":6,"newStart":586,"newLines":7,"lines":[" {","   Serial.begin(115200);","   delay(500);","+  randomSeed(esp_random());"," ","   // Le RX doit boucler en busy-wait (précision microseconde) sans jamais","   // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01HJ8dCq6qGiacNmwBcU3PWE","duration_ms":24,"timestamp":"2026-07-02 09:33:41"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif","new_string":"// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif","newString":"// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Message constant dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":44,"oldLines":13,"newStart":44,"newLines":11,"lines":[" volatile bool g_remoteNackReceived = false;"," volatile uint8_t g_remoteNackTargetSeq = 0;"," ","-// Message constant dépendant de l\u0027ESP_ID","+// Prefixes de log dépendant de l\u0027ESP_ID"," #if ESP_ID == 1","-const char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Kevin\";"," const char TX_PREFIX[] = \"TX1\";"," const char RX_PREFIX[] = \"RX1\";"," #else","-const char *MESSAGE_TO_SEND = \"Allo je m\u0027appelle Zach\";"," const char TX_PREFIX[] = \"TX2\";"," const char RX_PREFIX[] = \"RX2\";"," #endif"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01LEbT8WoKYnyFtqoSbARriv","duration_ms":18,"timestamp":"2026-07-02 09:33:51"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"d84d37ff-e33b-4634-9951-270cfc9c8de5","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================","new_string":"// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================","newString":"// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 2 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":379,"oldLines":10,"newStart":379,"newLines":6,"lines":[" }"," "," // ============================================================================","-// LOGIQUE APPLICATIVE","-// ============================================================================","-","-// ============================================================================"," // ACQUISITION DE DONNEES SIMULEE"," // ============================================================================"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_0114ybKFE9hvvRsUCXFHu5tf","duration_ms":17,"timestamp":"2026-07-02 09:34:02"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"C:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"41915e51-0410-4f91-abd9-458aa6bebc0e","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA","new_string":"#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA","newString":"#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":23,"oldLines":7,"newStart":23,"newLines":7,"lines":[" #define FRAME_TYPE_FIN 0x03"," #define FRAME_TYPE_NACK 0x04"," ","-#define BIT_ERROR_PROBABILITY_PCT 15 // % de chance d\u0027injecter une erreur par trame DATA","+#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)"," "," // Structure de trame"," typedef struct"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01RC83Z8B55psYjpUxQjTNzE","duration_ms":17,"timestamp":"2026-07-02 09:46:57"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"C:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"41915e51-0410-4f91-abd9-458aa6bebc0e","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","new_string":"// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","newString":"// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Simule une erreur de canal: inverse un bit aleatoire du payload deja\n// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible\n// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)\n  {\n    int byteIdx = random(0, f-\u003epayloadLen);\n    int bitIdx = random(0, 8);\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":413,"oldLines":18,"newStart":413,"newLines":17,"lines":["   return count;"," }"," ","-// Simule une erreur de canal: inverse un bit aleatoire du payload deja","-// protege par son CRC (calcule dans buildFrame), rendant l\u0027erreur invisible","-// a l\u0027emetteur et detectable uniquement par le CRC du recepteur.","+// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,","+// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo)."," void maybeInjectBitError(Frame *f)"," {","   if (!f || f-\u003epayloadLen == 0)","     return;"," ","-  if (random(0, 100) \u003c BIT_ERROR_PROBABILITY_PCT)","+  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)","   {","-    int byteIdx = random(0, f-\u003epayloadLen);","-    int bitIdx = random(0, 8);","+    int byteIdx = 0;","+    int bitIdx = 3;","     f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);","     logAppend(g_txLogBuf, \u0026g_txLogLen,","               \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\","]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01WpFqVySsyxyriXimfMh56S","duration_ms":15,"timestamp":"2026-07-02 09:47:06"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"a212b5fb-94b1-495f-978d-d200b36ac760","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"void sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}","new_string":"// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"void sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}","newString":"// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  delayMicroseconds(BIT_PERIOD_US);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":97,"oldLines":10,"newStart":97,"newLines":25,"lines":[" // ============================================================================"," // Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)"," ","+// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé","+// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead","+// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload","+// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres","+// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser","+// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une","+// trame de plusieurs centaines de bits.","+uint32_t g_txClockStart = 0;","+int g_txBitIndex = 0;","+"," void sendBit(bool bitValue)"," {","   digitalWrite(TX_PIN, bitValue ? HIGH : LOW);","-  delayMicroseconds(BIT_PERIOD_US);","+  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);","+  while (micros() \u003c targetTime)","+  {","+    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)","+  }","+  g_txBitIndex++;"," }"," "," void sendByte(uint8_t b)"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01LDLmsTXqJxV5fnAaKi6Ggw","duration_ms":16,"timestamp":"2026-07-02 09:50:10"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"a212b5fb-94b1-495f-978d-d200b36ac760","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"void sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);","new_string":"void sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"void sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);","newString":"void sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":284,"oldLines":6,"newStart":284,"newLines":9,"lines":["   if (!f)","     return;"," ","+  g_txClockStart = micros();","+  g_txBitIndex = 0;","+","   sendByte(PREAMBLE_BYTE);","   sendByte(START_BYTE);"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01WQofcGJDXtoynyaM8wT4MG","duration_ms":12,"timestamp":"2026-07-02 09:50:16"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"9b91e63b-58f6-4410-bad5-ec56adfb7a8d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","new_string":"      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","newString":"      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":547,"oldLines":8,"newStart":547,"newLines":13,"lines":["         g_localGapTargetSeq = g_rxExpectedSeq;","         g_localGapDetected = true;","       }","-      else if (assembledLen + rxFrame.payloadLen \u003c= (int)sizeof(assembledMsg) - 1)","+      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)","       {","+        if (assembledLen \u003e 0)","+        {","+          assembledMsg[assembledLen] = \u0027\\n\u0027;","+          assembledLen++;","+        }","         memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);","         assembledLen += rxFrame.payloadLen;","         g_rxExpectedSeq++;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01AHkqr6rRSMhJitqWyYuy4t","duration_ms":19,"timestamp":"2026-07-02 09:52:41"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"9b91e63b-58f6-4410-bad5-ec56adfb7a8d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }","new_string":"    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }","newString":"    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":475,"oldLines":7,"newStart":475,"newLines":8,"lines":["     {","       uint8_t target = g_remoteNackTargetSeq;","       g_remoteNackReceived = false;","-      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK recu, retour au paquet %d\\n\", target);","+      logAppend(g_txLogBuf, \u0026g_txLogLen,","+                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);","       i = target - 1;","       continue;","     }"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01ApMLg7VunhJBTL6pEyYE9y","duration_ms":23,"timestamp":"2026-07-02 09:52:50"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"9b91e63b-58f6-4410-bad5-ec56adfb7a8d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }","new_string":"    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }","newString":"    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":493,"oldLines":7,"newStart":493,"newLines":8,"lines":["       Frame fNack;","       buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);","       sendFrame(\u0026fNack);","-      logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] NACK envoye pour paquet %d\\n\", target);","+      logAppend(g_txLogBuf, \u0026g_txLogLen,","+                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);","     }"," ","     i++;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01KNdegSKUSDuv9ARnatQWa3","duration_ms":11,"timestamp":"2026-07-02 09:52:58"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"9b91e63b-58f6-4410-bad5-ec56adfb7a8d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","new_string":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","newString":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":537,"oldLines":7,"newStart":537,"newLines":8,"lines":["     {","       if (!rxFrame.crcValid)","       {","-        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] CRC invalide sur trame seq=%d -\u003e NACK(%d)\\n\",","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen,","+                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",","                   rxFrame.seqNum, g_rxExpectedSeq);","         g_localGapTargetSeq = g_rxExpectedSeq;","         g_localGapDetected = true;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_0117FcsGRxdQfGhqXAHTs7Yd","duration_ms":15,"timestamp":"2026-07-02 09:53:08"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"9b91e63b-58f6-4410-bad5-ec56adfb7a8d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","new_string":"#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","newString":"#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":23,"oldLines":7,"newStart":23,"newLines":7,"lines":[" #define FRAME_TYPE_FIN 0x03"," #define FRAME_TYPE_NACK 0x04"," ","-#define BIT_ERROR_PROBABILITY_PCT 0 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)","+#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)"," "," // Structure de trame"," typedef struct"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01HakPt1Z2jHGgfpx52b3c2X","duration_ms":18,"timestamp":"2026-07-02 09:53:15"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"2798baee-bc4d-43f7-908a-33c93c161aee","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"void maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","new_string":"bool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"void maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","newString":"bool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":433,"oldLines":16,"newStart":433,"newLines":19,"lines":[" "," // Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,"," // =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).","+bool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)","+"," void maybeInjectBitError(Frame *f)"," {","   if (!f || f-\u003epayloadLen == 0)","     return;"," ","-  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2)","+  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)","   {","     int byteIdx = 0;","     int bitIdx = 3;","     f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);","+    g_errorAlreadyInjected = true;","     logAppend(g_txLogBuf, \u0026g_txLogLen,","               \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",","               bitIdx, byteIdx, f-\u003eseqNum);"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01WMeUkGvD9t4D5kqWeFpEjJ","duration_ms":19,"timestamp":"2026-07-02 09:54:59"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"2798baee-bc4d-43f7-908a-33c93c161aee","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;","new_string":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;","newString":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":466,"oldLines":6,"newStart":466,"newLines":7,"lines":["     return;"," ","   logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);","+  g_errorAlreadyInjected = false;"," ","   Frame fBegin;","   buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01NgtqCUamdLjqBApyX2NGGE","duration_ms":16,"timestamp":"2026-07-02 09:55:05"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"492a5a44-5652-4e26-9521-7c8add76361d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","new_string":"      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }","newString":"      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n        g_localGapTargetSeq = g_rxExpectedSeq;\n        g_localGapDetected = true;\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":541,"oldLines":18,"newStart":541,"newLines":24,"lines":["     {","       if (!rxFrame.crcValid)","       {","-        logAppend(g_rxLogBuf, \u0026g_rxLogLen,","-                  \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",","-                  rxFrame.seqNum, g_rxExpectedSeq);","-        g_localGapTargetSeq = g_rxExpectedSeq;","-        g_localGapDetected = true;","+        if (!g_localGapDetected)","+        {","+          logAppend(g_rxLogBuf, \u0026g_rxLogLen,","+                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",","+                    rxFrame.seqNum, g_rxExpectedSeq);","+          g_localGapTargetSeq = g_rxExpectedSeq;","+          g_localGapDetected = true;","+        }","       }","       else if (rxFrame.seqNum != g_rxExpectedSeq)","       {","-        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",","-                  g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);","-        g_localGapTargetSeq = g_rxExpectedSeq;","-        g_localGapDetected = true;","+        if (!g_localGapDetected)","+        {","+          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",","+                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);","+          g_localGapTargetSeq = g_rxExpectedSeq;","+          g_localGapDetected = true;","+        }","       }","       else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)","       {"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01EgMYHJRV1ami5LcJcZVM2C","duration_ms":17,"timestamp":"2026-07-02 10:01:12"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"492a5a44-5652-4e26-9521-7c8add76361d","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}","new_string":"    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}","newString":"    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n      g_remoteNackTargetSeq = rxFrame.param;\n      g_remoteNackReceived = true;\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n    }\n\n    logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":575,"oldLines":23,"newStart":575,"newLines":26,"lines":["     else if (rxFrame.type == FRAME_TYPE_FIN)","     {","       assembledMsg[assembledLen] = \u0027\\0\u0027;","-      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois: \\\"%s\\\"\\n\", RX_PREFIX, assembledMsg);","+      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);","       memset(assembledMsg, 0, sizeof(assembledMsg));","+      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet","     }","     else if (rxFrame.type == FRAME_TYPE_NACK)","     {","-      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);","-      g_remoteNackTargetSeq = rxFrame.param;","-      g_remoteNackReceived = true;","+      if (!g_remoteNackReceived)","+      {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);","+        g_remoteNackTargetSeq = rxFrame.param;","+        g_remoteNackReceived = true;","+      }","     }","     else","     {","       logAppend(g_rxLogBuf, \u0026g_rxLogLen,","                 \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",","                 rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);","+      logFlush(g_rxLogBuf, \u0026g_rxLogLen);","     }","-","-    logFlush(g_rxLogBuf, \u0026g_rxLogLen);","   }"," }"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01QpSdRzgBGuBGW3FYSrQgG7","duration_ms":18,"timestamp":"2026-07-02 10:01:28"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","new_string":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}","newString":"  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);\n  g_errorAlreadyInjected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":465,"oldLines":8,"newStart":465,"newLines":10,"lines":["   if (totalPackets == 0)","     return;"," ","-  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[%s] J\u0027envoie %d paquets de donnees\\n\", TX_PREFIX, totalPackets);","+  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);","   g_errorAlreadyInjected = false;","+  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente","+  g_localGapDetected = false;"," ","   Frame fBegin;","   buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);"]},{"oldStart":480,"oldLines":13,"newStart":482,"newLines":14,"lines":["       uint8_t target = g_remoteNackTargetSeq;","       g_remoteNackReceived = false;","       logAppend(g_txLogBuf, \u0026g_txLogLen,","-                \"[TX] \u003e\u003e\u003e NACK recu du recepteur: retransmission a partir du paquet seq=%d \u003c\u003c\u003c\\n\", target);","+                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);","       i = target - 1;","       continue;","     }"," ","     Frame fData;","     buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);","+    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);","     maybeInjectBitError(\u0026fData);","     sendFrame(\u0026fData);"," "]},{"oldStart":498,"oldLines":7,"newStart":501,"newLines":7,"lines":["       buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);","       sendFrame(\u0026fNack);","       logAppend(g_txLogBuf, \u0026g_txLogLen,","-                \"[TX] \u003e\u003e\u003e Erreur detectee cote reception: demande de renvoi du paquet seq=%d \u003c\u003c\u003c\\n\", target);","+                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);","     }"," ","     i++;"]},{"oldStart":507,"oldLines":6,"newStart":510,"newLines":7,"lines":["   Frame fEnd;","   buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);","   sendFrame(\u0026fEnd);","+  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");"," }"," "," // ============================================================================"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_011jyrmxySY7BnYPogntFHCm","duration_ms":18,"timestamp":"2026-07-02 10:12:02"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);\n    maybeInjectBitError(\u0026fData);","new_string":"    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);\n    maybeInjectBitError(\u0026fData);","newString":"    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":489,"oldLines":7,"newStart":489,"newLines":7,"lines":[" ","     Frame fData;","     buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);","-    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, (const char *)packets[i]);","+    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));","     maybeInjectBitError(\u0026fData);","     sendFrame(\u0026fData);"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01QdCgz4Bv5TAD6x4EojGNJW","duration_ms":15,"timestamp":"2026-07-02 10:12:13"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }","new_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }","newString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",\n                    rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        if (!g_localGapDetected)\n        {\n          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",\n                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":539,"oldLines":33,"newStart":539,"newLines":36,"lines":["     {","       assembledLen = 0;","       g_rxExpectedSeq = 1;","+      g_localGapDetected = false;","       logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);","     }","     else if (rxFrame.type == FRAME_TYPE_DATA)","     {","       if (!rxFrame.crcValid)","       {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen,","+                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);","         if (!g_localGapDetected)","         {","-          logAppend(g_rxLogBuf, \u0026g_rxLogLen,","-                    \"[RX] !!! ERREUR DETECTEE sur trame seq=%d (CRC invalide) -\u003e demande de renvoi du paquet %d !!!\\n\",","-                    rxFrame.seqNum, g_rxExpectedSeq);","           g_localGapTargetSeq = g_rxExpectedSeq;","           g_localGapDetected = true;","         }","       }","       else if (rxFrame.seqNum != g_rxExpectedSeq)","       {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen,","+                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",","+                  rxFrame.seqNum, g_rxExpectedSeq);","         if (!g_localGapDetected)","         {","-          logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Desynchronisation: attendu %d, recu %d -\u003e NACK(%d)\\n\",","-                    g_rxExpectedSeq, rxFrame.seqNum, g_rxExpectedSeq);","           g_localGapTargetSeq = g_rxExpectedSeq;","           g_localGapDetected = true;","         }","       }","       else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)","       {","+        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",","+                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);","         if (assembledLen \u003e 0)","         {","           assembledMsg[assembledLen] = \u0027\\n\u0027;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01AgASWUdjN6L9o5vy2RgG97","duration_ms":9,"timestamp":"2026-07-02 10:12:33"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);","new_string":"      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);","newString":"      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":582,"oldLines":7,"newStart":582,"newLines":7,"lines":["     else if (rxFrame.type == FRAME_TYPE_FIN)","     {","       assembledMsg[assembledLen] = \u0027\\0\u0027;","-      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[%s] Je reçois:\\n%s\\n\", RX_PREFIX, assembledMsg);","+      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);","       memset(assembledMsg, 0, sizeof(assembledMsg));","       logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet","     }"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01RvvRJFJwJmeaZdmwkDqHL6","duration_ms":18,"timestamp":"2026-07-02 10:12:42"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n","new_string":"","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n","newString":"","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// Prefixes de log dépendant de l\u0027ESP_ID\n#if ESP_ID == 1\nconst char TX_PREFIX[] = \"TX1\";\nconst char RX_PREFIX[] = \"RX1\";\n#else\nconst char TX_PREFIX[] = \"TX2\";\nconst char RX_PREFIX[] = \"RX2\";\n#endif\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":44,"oldLines":15,"newStart":44,"newLines":6,"lines":[" volatile bool g_remoteNackReceived = false;"," volatile uint8_t g_remoteNackTargetSeq = 0;"," ","-// Prefixes de log dépendant de l\u0027ESP_ID","-#if ESP_ID == 1","-const char TX_PREFIX[] = \"TX1\";","-const char RX_PREFIX[] = \"RX1\";","-#else","-const char TX_PREFIX[] = \"TX2\";","-const char RX_PREFIX[] = \"RX2\";","-#endif","-"," // ============================================================================"," // LOGS DIFFÉRÉS"," // ============================================================================"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_016acKSqH1wM9JdqvZ7T94Ga","duration_ms":11,"timestamp":"2026-07-02 10:12:52"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"b39584c2-0d34-4425-afc9-d39ee429257c","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);","new_string":"      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);","newString":"      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":492,"oldLines":7,"newStart":492,"newLines":7,"lines":["       buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);","       sendFrame(\u0026fNack);","       logAppend(g_txLogBuf, \u0026g_txLogLen,","-                \"[TX] Demande de renvoi recue du recepteur -\u003e j\u0027envoie un NACK pour la trame %d\\n\", target);","+                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);","     }"," ","     i++;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_012tSooxuQsVJFNit6skkLVK","duration_ms":17,"timestamp":"2026-07-02 10:13:05"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"6ddeb985-2dc9-4629-8d27-1a40edb340ae","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;","new_string":"// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;","newString":"// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":43,"oldLines":6,"newStart":43,"newLines":9,"lines":[" volatile uint8_t g_localGapTargetSeq = 0;"," volatile bool g_remoteNackReceived = false;"," volatile uint8_t g_remoteNackTargetSeq = 0;","+// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que","+// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.","+bool g_nackAlreadySentForEpisode = false;"," "," // ============================================================================"," // LOGS DIFFÉRÉS"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_012UFgqxvnmmnSfXj2MPYfVA","duration_ms":18,"timestamp":"2026-07-02 10:17:03"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"6ddeb985-2dc9-4629-8d27-1a40edb340ae","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","new_string":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }","newString":"      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Ser\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_localGapDetected)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":543,"oldLines":10,"newStart":543,"newLines":11,"lines":["       {","         logAppend(g_rxLogBuf, \u0026g_rxLogLen,","                   \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);","-        if (!g_localGapDetected)","+        if (!g_nackAlreadySentForEpisode)","         {","           g_localGapTargetSeq = g_rxExpectedSeq;","           g_localGapDetected = true;","+          g_nackAlreadySentForEpisode = true;","         }","       }","       else if (rxFrame.seqNum != g_rxExpectedSeq)"]},{"oldStart":554,"oldLines":10,"newStart":555,"newLines":11,"lines":["         logAppend(g_rxLogBuf, \u0026g_rxLogLen,","                   \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",","                   rxFrame.seqNum, g_rxExpectedSeq);","-        if (!g_localGapDetected)","+        if (!g_nackAlreadySentForEpisode)","         {","           g_localGapTargetSeq = g_rxExpectedSeq;","           g_localGapDetected = true;","+          g_nackAlreadySentForEpisode = true;","         }","       }","       else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)"]},{"oldStart":572,"oldLines":6,"newStart":574,"newLines":7,"lines":["         memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);","         assembledLen += rxFrame.payloadLen;","         g_rxExpectedSeq++;","+        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu","       }","     }","     else if (rxFrame.type == FRAME_TYPE_FIN)"]}],"userModified":false,"replaceAll":false,"staleRecovered":true},"tool_use_id":"toolu_01HmsC1ZkwJg36PoDMZLfHoC","duration_ms":26,"timestamp":"2026-07-02 10:17:16"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"6ddeb985-2dc9-4629-8d27-1a40edb340ae","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }","new_string":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }","newString":"    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println(\"-------------------------------------------------\");\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":535,"oldLines":6,"newStart":535,"newLines":7,"lines":["       assembledLen = 0;","       g_rxExpectedSeq = 1;","       g_localGapDetected = false;","+      g_nackAlreadySentForEpisode = false;","       logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);","     }","     else if (rxFrame.type == FRAME_TYPE_DATA)"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01Xv7u2LcbEgphmhscziokqq","duration_ms":19,"timestamp":"2026-07-02 10:17:40"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"6006b105-62fb-4dbb-9520-669c9921fb76","permission_mode":"acceptEdits","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"#define LOG_BUF_SIZE 1024","new_string":"#define LOG_BUF_SIZE 3072","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"#define LOG_BUF_SIZE 1024","newString":"#define LOG_BUF_SIZE 3072","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 1024\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":58,"oldLines":7,"newStart":58,"newLines":7,"lines":[" // dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,"," // juste avant l\u0027attente entre deux envois/réceptions."," ","-#define LOG_BUF_SIZE 1024","+#define LOG_BUF_SIZE 3072"," char g_txLogBuf[LOG_BUF_SIZE];"," size_t g_txLogLen = 0;"," char g_rxLogBuf[LOG_BUF_SIZE];"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01Vufg9NqPBa8C223Z6VUQhE","duration_ms":18,"timestamp":"2026-07-02 10:23:20"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}","new_string":"// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits\nuint32_t g_rxClockStart = 0;\nint g_rxHalfBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}","newString":"// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits\nuint32_t g_rxClockStart = 0;\nint g_rxHalfBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE NRZ BAS NIVEAU\n// ============================================================================\n// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload\n// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres\n// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser\n// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une\n// trame de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txBitIndex = 0;\n\nvoid sendBit(bool bitValue)\n{\n  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txBitIndex++;\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)\n  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);\n\n  // Attendre le moment d\u0027échantillonnage\n  while (micros() \u003c sampleTime)\n  {\n    // Spin jusqu\u0027au moment d\u0027échantillonner\n  }\n\n  *outBit = digitalRead(RX_PIN) == HIGH;\n\n  // Attendre la fin du bit\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre le prochain bit\n  }\n\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":89,"oldLines":31,"newStart":89,"newLines":41,"lines":[" }"," "," // ============================================================================","-// ENCODAGE NRZ BAS NIVEAU","+// ENCODAGE MANCHESTER BAS NIVEAU"," // ============================================================================","-// Convention NRZ simple : 0 = LOW (maintenu 1ms), 1 = HIGH (maintenu 1ms)","+// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition","+// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante","+// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux","+// demi-periodes de BIT_PERIOD_US/2 chacune."," "," // Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé"," // une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead","-// de boucle ne s\u0027accumule pas bit apres bit sur les trames longues (payload","-// jusqu\u0027a 80 octets = ~700 bits). Un delai relatif (delayMicroseconds apres","-// digitalWrite) derive de quelques us par bit, ce qui suffit a desynchroniser","-// le recepteur (horloge absolue, lui aussi figee une seule fois) sur une","-// trame de plusieurs centaines de bits.","+// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues","+// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif","+// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le","+// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame","+// de plusieurs centaines de bits."," uint32_t g_txClockStart = 0;","-int g_txBitIndex = 0;","+int g_txHalfBitIndex = 0;"," ","-void sendBit(bool bitValue)","+void sendHalfBit(bool levelHigh)"," {","-  digitalWrite(TX_PIN, bitValue ? HIGH : LOW);","-  uint32_t targetTime = g_txClockStart + ((g_txBitIndex + 1) * (uint32_t)BIT_PERIOD_US);","+  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);","+  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));","   while (micros() \u003c targetTime)","   {","-    // Spin jusqu\u0027a la fin exacte du bit (horloge absolue, pas de derive cumulee)","+    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)","   }","-  g_txBitIndex++;","+  g_txHalfBitIndex++;"," }"," ","+void sendBit(bool bitValue)","+{","+  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH","+  sendHalfBit(bitValue ? HIGH : LOW);","+  sendHalfBit(bitValue ? LOW : HIGH);","+}","+"," void sendByte(uint8_t b)"," {","   for (int i = 7; i \u003e= 0; i--)"]},{"oldStart":122,"oldLines":34,"newStart":132,"newLines":48,"lines":["   }"," }"," ","-// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des bits","+// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits"," uint32_t g_rxClockStart = 0;","-int g_rxBitIndex = 0;","+int g_rxHalfBitIndex = 0;"," ","-// Lit un bit en utilisant l\u0027horloge globale d\u0027échantillonnage","+// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau","+// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second","+// demi-bit est lu seulement pour verifier la transition attendue (integrite)."," bool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)"," {","   if (!outBit)","     return false;"," ","-  // Calculer le moment d\u0027échantillonnage pour ce bit (au centre du bit)","-  uint32_t sampleTime = g_rxClockStart + (bitIndex * bitPeriodUs) + (bitPeriodUs / 2);","-  uint32_t timeoutTime = g_rxClockStart + ((bitIndex + 1) * bitPeriodUs);","+  uint32_t halfPeriodUs = bitPeriodUs / 2;","+  int firstHalfIndex = bitIndex * 2;"," ","-  // Attendre le moment d\u0027échantillonnage","-  while (micros() \u003c sampleTime)","+  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);","+  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);","+  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);","+","+  while (micros() \u003c sampleTime1)","   {","-    // Spin jusqu\u0027au moment d\u0027échantillonner","+    // Spin jusqu\u0027au centre du premier demi-bit","   }","+  bool firstHalf = digitalRead(RX_PIN) == HIGH;"," ","-  *outBit = digitalRead(RX_PIN) == HIGH;","+  while (micros() \u003c sampleTime2)","+  {","+    // Spin jusqu\u0027au centre du second demi-bit","+  }","+  bool secondHalf = digitalRead(RX_PIN) == HIGH;"," ","-  // Attendre la fin du bit","   while (micros() \u003c timeoutTime)","   {","-    // Attendre le prochain bit","+    // Attendre la fin exacte du bit","   }"," ","+  if (firstHalf == secondHalf)","+  {","+    return false; // pas de transition au centre: viole le codage Manchester","+  }","+","+  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0","   return true;"," }"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01C8y8HHMT7jBUSETDKqJKhC","duration_ms":29,"timestamp":"2026-07-03 23:39:33"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits\nuint32_t g_rxClockStart = 0;\nint g_rxHalfBitIndex = 0;","new_string":"// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits\nuint32_t g_rxClockStart = 0;\nint g_rxHalfBitIndex = 0;","newString":"// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits\nuint32_t g_rxClockStart = 0;\nint g_rxHalfBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":132,"oldLines":9,"newStart":132,"newLines":10,"lines":["   }"," }"," ","-// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage des demi-bits","+// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;","+// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)"," uint32_t g_rxClockStart = 0;","-int g_rxHalfBitIndex = 0;","+int g_rxBitIndex = 0;"," "," // Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau"," // du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01K6unQCFcQGeCgej8ikDsDN","duration_ms":23,"timestamp":"2026-07-03 23:39:47"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;","new_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;","newString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE\n// ============================================================================\n// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous\n// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a\n// PAS de front au tout début du préambule : le premier front utile est la\n// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE\n// front précis, on calcule le centre du bit1 à partir de là, puis on relit\n// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,\n// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie\n// entièrement au lieu d\u0027abandonner.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 20;\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,\n    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(50);\n    }\n\n    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(50);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B\n    }\n\n    uint32_t frontTime = micros();\n    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);\n\n    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.\n    uint8_t preambleByte = 0;\n    uint32_t sampleTime = bit1Center;\n\n    for (int i = 1; i \u003c 8; i++)\n    {\n      while (micros() \u003c sampleTime)\n      {\n        // Spin (précision microseconde requise pour l\u0027échantillonnage)\n      }\n      bool bit = (digitalRead(RX_PIN) == HIGH);\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      sampleTime += bitPeriodUs;\n    }\n\n    if (preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain\n    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.\n    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2\n    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);\n    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)\n\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":199,"oldLines":72,"newStart":199,"newLines":72,"lines":[" }"," "," // ============================================================================","-// CALIBRATION DU PRÉAMBULE","+// CALIBRATION DU PRÉAMBULE (Manchester)"," // ============================================================================","-// Le préambule 0x55 (01010101) alterne à chaque bit en NRZ =\u003e transition tous","-// les ~1ms. Ligne idle = LOW. 0x55 commence par un bit 0 (LOW), donc il n\u0027y a","-// PAS de front au tout début du préambule : le premier front utile est la","-// transition LOW-\u003eHIGH entre le bit0 (0) et le bit1 (1). On s\u0027accroche à CE","-// front précis, on calcule le centre du bit1 à partir de là, puis on relit","-// les 8 bits pour valider que c\u0027est bien 0x55. Si la validation échoue (glitch,","-// front capté au milieu d\u0027un préambule déjà en cours, bruit), on réessaie","-// entièrement au lieu d\u0027abandonner.","+// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE","+// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-","+// periodes au meme niveau). On s\u0027accroche donc simplement au premier front","+// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une","+// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien","+// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes","+// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement."," bool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)"," {","-  const int maxAttempts = 20;","+  const int maxAttempts = 40;","+  uint32_t halfPeriodUs = bitPeriodUs / 2;","+","   for (int attempt = 0; attempt \u003c maxAttempts; attempt++)","   {","-    // Étape A: s\u0027assurer que la ligne est idle (LOW) avant d\u0027armer la détection,","-    // pour éviter de s\u0027accrocher à un front au milieu d\u0027une trame déjà en cours.","-    while (digitalRead(RX_PIN) == HIGH)","-    {","-      esp_rom_delay_us(50);","-    }","+    bool idleLevel = digitalRead(RX_PIN) == HIGH;"," ","-    // Étape B: attendre le front montant LOW-\u003eHIGH (transition bit0-\u003ebit1 du 0x55)","+    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.","     uint32_t armStart = micros();","-    while (digitalRead(RX_PIN) == LOW)","+    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)","     {","       if ((micros() - armStart) \u003e 2000000UL)","       {","         break; // rien reçu depuis 2s: on relance une nouvelle tentative propre","       }","-      esp_rom_delay_us(50);","+      esp_rom_delay_us(5);","     }","-    if (digitalRead(RX_PIN) == LOW)","+    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)","     {","-      continue; // timeout d\u0027armement, on recommence l\u0027étape A/B","+      continue; // timeout d\u0027armement, on recommence","     }"," ","     uint32_t frontTime = micros();","-    uint32_t bit1Center = frontTime + (bitPeriodUs / 2);"," ","-    // On relit les bits 1 à 7 (le bit0 est implicite = 0) pour valider le motif.","-    uint8_t preambleByte = 0;","-    uint32_t sampleTime = bit1Center;","+    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,","+    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe","+    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir","+    // de la et on relit les 8 bits pour valider l\u0027hypothese.","+    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);","+    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);"," ","-    for (int i = 1; i \u003c 8; i++)","+    g_rxClockStart = candidateClockStart;","+    g_rxBitIndex = 0;","+","+    uint8_t preambleByte = 0;","+    bool valid = true;","+    for (int i = 0; i \u003c 8; i++)","     {","-      while (micros() \u003c sampleTime)","+      bool bit;","+      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))","       {","-        // Spin (précision microseconde requise pour l\u0027échantillonnage)","+        valid = false;","+        break;","       }","-      bool bit = (digitalRead(RX_PIN) == HIGH);","       preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);","-      sampleTime += bitPeriodUs;","+      g_rxBitIndex++;","     }"," ","-    if (preambleByte != PREAMBLE_BYTE)","+    if (!valid || preambleByte != PREAMBLE_BYTE)","     {","       continue;","     }"," ","-    // Initialiser l\u0027horloge globale. Le bit8 (START_BYTE bit0) est le prochain","-    // bit après le bit7 du préambule. sampleTime pointe déjà au centre du bit8.","-    // g_rxClockStart doit satisfaire: sampleTime(bitIndex) = g_rxClockStart + bitIndex*bitPeriodUs + bitPeriodUs/2","-    g_rxClockStart = sampleTime - (8 * bitPeriodUs) - (bitPeriodUs / 2);","-    g_rxBitIndex = 8; // Prochain bit à lire = bit 8 (START_BYTE)","-","+    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire","+    // est le bit8 (START_BYTE bit0).","     return true;","   }"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01Xtnnd6YkVEetZaipeqogyC","duration_ms":32,"timestamp":"2026-07-03 23:40:21"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","new_string":"  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","newString":"  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":306,"oldLines":7,"newStart":306,"newLines":7,"lines":["     return;"," ","   g_txClockStart = micros();","-  g_txBitIndex = 0;","+  g_txHalfBitIndex = 0;"," ","   sendByte(PREAMBLE_BYTE);","   sendByte(START_BYTE);"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01BZaVzeUizeZHewTP54xQS5","duration_ms":27,"timestamp":"2026-07-03 23:40:35"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"#define BIT_PERIOD_US 1000 // 1 bit = 1ms","new_string":"#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"#define BIT_PERIOD_US 1000 // 1 bit = 1ms","newString":"#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // 1 bit = 1ms\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":11,"oldLines":7,"newStart":11,"newLines":7,"lines":[" "," #define TX_PIN 4"," #define RX_PIN 5","-#define BIT_PERIOD_US 1000 // 1 bit = 1ms","+#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)"," "," #define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation"," #define START_BYTE 0x7E    // 01111110 -- délimiteur début"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01PKEXwvV6Zr4iz3rRhr7dgY","duration_ms":29,"timestamp":"2026-07-03 23:40:50"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"8d2012b6-7000-49b7-acdd-d9a40c589abb","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;","new_string":"    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le\n    // debut theorique de la trame (g_rxClockStart). On reconstruit toute\n    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.\n    g_rxClockStart = frontTime - halfPeriodUs;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;","newString":"    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le\n    // debut theorique de la trame (g_rxClockStart). On reconstruit toute\n    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.\n    g_rxClockStart = frontTime - halfPeriodUs;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe\n    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir\n    // de la et on relit les 8 bits pour valider l\u0027hypothese.\n    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);\n    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);\n\n    g_rxClockStart = candidateClockStart;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":235,"oldLines":13,"newStart":235,"newLines":10,"lines":["     uint32_t frontTime = micros();"," ","     // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,","-    // car le bit0 de 0x55 = 0). Le centre du 1er demi-bit du bit0 est donc situe","-    // une demi-periode AVANT ce front. On reconstruit toute l\u0027horloge a partir","-    // de la et on relit les 8 bits pour valider l\u0027hypothese.","-    uint32_t bit0FirstHalfCenter = frontTime - halfPeriodUs + (halfPeriodUs / 2);","-    uint32_t candidateClockStart = bit0FirstHalfCenter - (halfPeriodUs / 2);","-","-    g_rxClockStart = candidateClockStart;","+    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le","+    // debut theorique de la trame (g_rxClockStart). On reconstruit toute","+    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.","+    g_rxClockStart = frontTime - halfPeriodUs;","     g_rxBitIndex = 0;"," ","     uint8_t preambleByte = 0;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01WRu3T2srYsGat1FcByMh61","duration_ms":23,"timestamp":"2026-07-03 23:41:05"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"889704be-7de6-4aa3-9ad3-1a504bbbe01f","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le\n    // debut theorique de la trame (g_rxClockStart). On reconstruit toute\n    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;","new_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front\n// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et\n// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la\n// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur\n// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps\n// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique\n// front capte comme reference et on scanne les decalages via une nouvelle\n// tentative d\u0027armement a chaque echec -- statistiquement, un front different\n// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui\n// finit par tomber sur la bonne phase en au plus quelques tentatives.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul\n    // par rapport au front capte) pour placer g_rxClockStart. Une seule est\n    // correcte; les autres feront echouer la lecture Manchester (pas de\n    // transition au centre attendu) ou donneront un octet different de 0x55.\n    for (int phase = 0; phase \u003c 16; phase++)\n    {\n      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);\n      g_rxBitIndex = 0;\n\n      // Verifie que cette hypothese de phase est compatible avec le front deja\n      // observe: le niveau juste avant frontTime doit correspondre au niveau\n      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut\n      // pas relire le passe, on valide uniquement via la relecture des 8 bits\n      // ci-dessous, qui echouera naturellement si la phase est fausse.\n      uint8_t preambleByte = 0;\n      bool valid = true;\n      for (int i = 0; i \u003c 8; i++)\n      {\n        bool bit;\n        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n        {\n          valid = false;\n          break;\n        }\n        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n        g_rxBitIndex++;\n      }\n\n      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n      {\n        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n        // est le bit8 (START_BYTE bit0).\n        return true;\n      }\n\n      // Cette phase a echoue: on a consomme du temps reel en attendant les\n      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une\n      // autre phase sur CE front: on sort et on rearme sur le prochain front.\n      break;\n    }\n  }\n\n  return false;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le\n    // debut theorique de la trame (g_rxClockStart). On reconstruit toute\n    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;","newString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front\n// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et\n// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la\n// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur\n// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps\n// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique\n// front capte comme reference et on scanne les decalages via une nouvelle\n// tentative d\u0027armement a chaque echec -- statistiquement, un front different\n// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui\n// finit par tomber sur la bonne phase en au plus quelques tentatives.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul\n    // par rapport au front capte) pour placer g_rxClockStart. Une seule est\n    // correcte; les autres feront echouer la lecture Manchester (pas de\n    // transition au centre attendu) ou donneront un octet different de 0x55.\n    for (int phase = 0; phase \u003c 16; phase++)\n    {\n      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);\n      g_rxBitIndex = 0;\n\n      // Verifie que cette hypothese de phase est compatible avec le front deja\n      // observe: le niveau juste avant frontTime doit correspondre au niveau\n      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut\n      // pas relire le passe, on valide uniquement via la relecture des 8 bits\n      // ci-dessous, qui echouera naturellement si la phase est fausse.\n      uint8_t preambleByte = 0;\n      bool valid = true;\n      for (int i = 0; i \u003c 8; i++)\n      {\n        bool bit;\n        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n        {\n          valid = false;\n          break;\n        }\n        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n        g_rxBitIndex++;\n      }\n\n      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n      {\n        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n        // est le bit8 (START_BYTE bit0).\n        return true;\n      }\n\n      // Cette phase a echoue: on a consomme du temps reel en attendant les\n      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une\n      // autre phase sur CE front: on sort et on rearme sur le prochain front.\n      break;\n    }\n  }\n\n  return false;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE\n// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau). On s\u0027accroche donc simplement au premier front\n// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une\n// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien\n// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes\n// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 40;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,\n    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le\n    // debut theorique de la trame (g_rxClockStart). On reconstruit toute\n    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (!valid || preambleByte != PREAMBLE_BYTE)\n    {\n      continue;\n    }\n\n    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire\n    // est le bit8 (START_BYTE bit0).\n    return true;\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":201,"oldLines":16,"newStart":201,"newLines":21,"lines":[" // ============================================================================"," // CALIBRATION DU PRÉAMBULE (Manchester)"," // ============================================================================","-// Le préambule 0x55 (01010101) produit en Manchester une transition à CHAQUE","-// demi-periode sauf a une frontiere sur deux entre bits (plateau de 2 demi-","-// periodes au meme niveau). On s\u0027accroche donc simplement au premier front","-// trouve sur la ligne (n\u0027importe lequel), on suppose qu\u0027il tombe pile sur une","-// frontiere de demi-bit, puis on relit les 8 bits pour valider que c\u0027est bien","-// 0x55 avec cette hypothese de phase. Si la validation echoue (le front captes","-// tombait au milieu d\u0027un bit, pas a sa frontiere), on reessaie entierement.","+// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque","+// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-","+// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front","+// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et","+// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la","+// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur","+// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps","+// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique","+// front capte comme reference et on scanne les decalages via une nouvelle","+// tentative d\u0027armement a chaque echec -- statistiquement, un front different","+// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui","+// finit par tomber sur la bonne phase en au plus quelques tentatives."," bool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)"," {","-  const int maxAttempts = 40;","+  const int maxAttempts = 60;","   uint32_t halfPeriodUs = bitPeriodUs / 2;"," ","   for (int attempt = 0; attempt \u003c maxAttempts; attempt++)"]},{"oldStart":234,"oldLines":35,"newStart":239,"newLines":46,"lines":[" ","     uint32_t frontTime = micros();"," ","-    // On suppose que ce front est la frontiere du 2e demi-bit du bit0 (LOW-\u003eHIGH,","-    // car le bit0 de 0x55 = 0), donc situe exactement une demi-periode apres le","-    // debut theorique de la trame (g_rxClockStart). On reconstruit toute","-    // l\u0027horloge a partir de la et on relit les 8 bits pour valider l\u0027hypothese.","-    g_rxClockStart = frontTime - halfPeriodUs;","-    g_rxBitIndex = 0;","-","-    uint8_t preambleByte = 0;","-    bool valid = true;","-    for (int i = 0; i \u003c 8; i++)","+    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul","+    // par rapport au front capte) pour placer g_rxClockStart. Une seule est","+    // correcte; les autres feront echouer la lecture Manchester (pas de","+    // transition au centre attendu) ou donneront un octet different de 0x55.","+    for (int phase = 0; phase \u003c 16; phase++)","     {","-      bool bit;","-      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))","+      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);","+      g_rxBitIndex = 0;","+","+      // Verifie que cette hypothese de phase est compatible avec le front deja","+      // observe: le niveau juste avant frontTime doit correspondre au niveau","+      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut","+      // pas relire le passe, on valide uniquement via la relecture des 8 bits","+      // ci-dessous, qui echouera naturellement si la phase est fausse.","+      uint8_t preambleByte = 0;","+      bool valid = true;","+      for (int i = 0; i \u003c 8; i++)","       {","-        valid = false;","-        break;","+        bool bit;","+        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))","+        {","+          valid = false;","+          break;","+        }","+        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);","+        g_rxBitIndex++;","       }","-      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);","-      g_rxBitIndex++;","-    }"," ","-    if (!valid || preambleByte != PREAMBLE_BYTE)","-    {","-      continue;","-    }","+      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)","+      {","+        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire","+        // est le bit8 (START_BYTE bit0).","+        return true;","+      }"," ","-    // g_rxClockStart/g_rxBitIndex sont deja corrects: le prochain bit a lire","-    // est le bit8 (START_BYTE bit0).","-    return true;","+      // Cette phase a echoue: on a consomme du temps reel en attendant les","+      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une","+      // autre phase sur CE front: on sort et on rearme sur le prochain front.","+      break;","+    }","   }"," ","   return false;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01ErgEFHYmX2hW84X5Co14v2","duration_ms":23,"timestamp":"2026-07-03 23:44:44"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"889704be-7de6-4aa3-9ad3-1a504bbbe01f","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front\n// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et\n// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la\n// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur\n// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps\n// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique\n// front capte comme reference et on scanne les decalages via une nouvelle\n// tentative d\u0027armement a chaque echec -- statistiquement, un front different\n// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui\n// finit par tomber sur la bonne phase en au plus quelques tentatives.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul\n    // par rapport au front capte) pour placer g_rxClockStart. Une seule est\n    // correcte; les autres feront echouer la lecture Manchester (pas de\n    // transition au centre attendu) ou donneront un octet different de 0x55.\n    for (int phase = 0; phase \u003c 16; phase++)\n    {\n      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);\n      g_rxBitIndex = 0;\n\n      // Verifie que cette hypothese de phase est compatible avec le front deja\n      // observe: le niveau juste avant frontTime doit correspondre au niveau\n      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut\n      // pas relire le passe, on valide uniquement via la relecture des 8 bits\n      // ci-dessous, qui echouera naturellement si la phase est fausse.\n      uint8_t preambleByte = 0;\n      bool valid = true;\n      for (int i = 0; i \u003c 8; i++)\n      {\n        bool bit;\n        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n        {\n          valid = false;\n          break;\n        }\n        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n        g_rxBitIndex++;\n      }\n\n      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n      {\n        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n        // est le bit8 (START_BYTE bit0).\n        return true;\n      }\n\n      // Cette phase a echoue: on a consomme du temps reel en attendant les\n      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une\n      // autre phase sur CE front: on sort et on rearme sur le prochain front.\n      break;\n    }\n  }\n\n  return false;\n}","new_string":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit\n    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la\n    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front\n// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et\n// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la\n// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur\n// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps\n// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique\n// front capte comme reference et on scanne les decalages via une nouvelle\n// tentative d\u0027armement a chaque echec -- statistiquement, un front different\n// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui\n// finit par tomber sur la bonne phase en au plus quelques tentatives.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul\n    // par rapport au front capte) pour placer g_rxClockStart. Une seule est\n    // correcte; les autres feront echouer la lecture Manchester (pas de\n    // transition au centre attendu) ou donneront un octet different de 0x55.\n    for (int phase = 0; phase \u003c 16; phase++)\n    {\n      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);\n      g_rxBitIndex = 0;\n\n      // Verifie que cette hypothese de phase est compatible avec le front deja\n      // observe: le niveau juste avant frontTime doit correspondre au niveau\n      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut\n      // pas relire le passe, on valide uniquement via la relecture des 8 bits\n      // ci-dessous, qui echouera naturellement si la phase est fausse.\n      uint8_t preambleByte = 0;\n      bool valid = true;\n      for (int i = 0; i \u003c 8; i++)\n      {\n        bool bit;\n        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n        {\n          valid = false;\n          break;\n        }\n        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n        g_rxBitIndex++;\n      }\n\n      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n      {\n        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n        // est le bit8 (START_BYTE bit0).\n        return true;\n      }\n\n      // Cette phase a echoue: on a consomme du temps reel en attendant les\n      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une\n      // autre phase sur CE front: on sort et on rearme sur le prochain front.\n      break;\n    }\n  }\n\n  return false;\n}","newString":"// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit\n    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la\n    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front\n// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et\n// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la\n// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur\n// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps\n// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique\n// front capte comme reference et on scanne les decalages via une nouvelle\n// tentative d\u0027armement a chaque echec -- statistiquement, un front different\n// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui\n// finit par tomber sur la bonne phase en au plus quelques tentatives.\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul\n    // par rapport au front capte) pour placer g_rxClockStart. Une seule est\n    // correcte; les autres feront echouer la lecture Manchester (pas de\n    // transition au centre attendu) ou donneront un octet different de 0x55.\n    for (int phase = 0; phase \u003c 16; phase++)\n    {\n      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);\n      g_rxBitIndex = 0;\n\n      // Verifie que cette hypothese de phase est compatible avec le front deja\n      // observe: le niveau juste avant frontTime doit correspondre au niveau\n      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut\n      // pas relire le passe, on valide uniquement via la relecture des 8 bits\n      // ci-dessous, qui echouera naturellement si la phase est fausse.\n      uint8_t preambleByte = 0;\n      bool valid = true;\n      for (int i = 0; i \u003c 8; i++)\n      {\n        bool bit;\n        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n        {\n          valid = false;\n          break;\n        }\n        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n        g_rxBitIndex++;\n      }\n\n      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n      {\n        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n        // est le bit8 (START_BYTE bit0).\n        return true;\n      }\n\n      // Cette phase a echoue: on a consomme du temps reel en attendant les\n      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une\n      // autre phase sur CE front: on sort et on rearme sur le prochain front.\n      break;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":203,"oldLines":16,"newStart":203,"newLines":15,"lines":[" // ============================================================================"," // Le préambule 0x55 (01010101) produit en Manchester une transition a chaque"," // demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-","-// periodes au meme niveau) -- il y a donc 16 phases possibles pour le front","-// qu\u0027on va capter en premier. On s\u0027accroche a un front (n\u0027importe lequel), et","-// on essaie chaque hypothese de decalage de phase (0..15 demi-periodes) en la","-// rejouant sur les bits DEJA ecoules depuis le front (donc sans bloquer sur","-// le futur): on lit l\u0027etat actuel de la ligne echantillonne au fil du temps","-// n\u0027est pas possible retroactivement, donc a la place on utilise l\u0027unique","-// front capte comme reference et on scanne les decalages via une nouvelle","-// tentative d\u0027armement a chaque echec -- statistiquement, un front different","-// est capte a chaque tentative (le prochain front reel sur la ligne), ce qui","-// finit par tomber sur la bonne phase en au plus quelques tentatives.","+// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le","+// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de","+// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un","+// front, on suppose une phase (le front = fin du demi-bit courant), puis on","+// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait","+// de la relecture a deja consomme le temps reel correspondant: on ne peut pas","+// revenir en arriere, donc on rearme simplement sur le prochain front reel","+// (qui, statistiquement, finit par tomber sur la bonne phase en quelques","+// tentatives grace au bruit/gigue naturel de detection)."," bool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)"," {","   const int maxAttempts = 60;"]},{"oldStart":239,"oldLines":45,"newStart":238,"newLines":31,"lines":[" ","     uint32_t frontTime = micros();"," ","-    // On essaie toutes les phases possibles (0 a 15 demi-periodes de recul","-    // par rapport au front capte) pour placer g_rxClockStart. Une seule est","-    // correcte; les autres feront echouer la lecture Manchester (pas de","-    // transition au centre attendu) ou donneront un octet different de 0x55.","-    for (int phase = 0; phase \u003c 16; phase++)","-    {","-      g_rxClockStart = frontTime - ((uint32_t)phase * halfPeriodUs) - (halfPeriodUs / 2);","-      g_rxBitIndex = 0;","+    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit","+    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la","+    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.","+    g_rxClockStart = frontTime - halfPeriodUs;","+    g_rxBitIndex = 0;"," ","-      // Verifie que cette hypothese de phase est compatible avec le front deja","-      // observe: le niveau juste avant frontTime doit correspondre au niveau","-      // attendu juste avant la frontiere de demi-bit choisie. Comme on ne peut","-      // pas relire le passe, on valide uniquement via la relecture des 8 bits","-      // ci-dessous, qui echouera naturellement si la phase est fausse.","-      uint8_t preambleByte = 0;","-      bool valid = true;","-      for (int i = 0; i \u003c 8; i++)","+    uint8_t preambleByte = 0;","+    bool valid = true;","+    for (int i = 0; i \u003c 8; i++)","+    {","+      bool bit;","+      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))","       {","-        bool bit;","-        if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))","-        {","-          valid = false;","-          break;","-        }","-        preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);","-        g_rxBitIndex++;","+        valid = false;","+        break;","       }","+      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);","+      g_rxBitIndex++;","+    }"," ","-      if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)","-      {","-        // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire","-        // est le bit8 (START_BYTE bit0).","-        return true;","-      }","-","-      // Cette phase a echoue: on a consomme du temps reel en attendant les","-      // bits (busy-wait), la ligne a donc avance. On ne peut plus tester une","-      // autre phase sur CE front: on sort et on rearme sur le prochain front.","-      break;","+    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)","+    {","+      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire","+      // est le bit8 (START_BYTE bit0).","+      return true;","     }","   }"," "]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01PjtJuHM52vRhpvRuSKsTeV","duration_ms":22,"timestamp":"2026-07-03 23:45:27"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"889704be-7de6-4aa3-9ad3-1a504bbbe01f","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit\n    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la\n    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;","new_string":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit\n    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la\n    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;","newString":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 500 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit\n    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la\n    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.\n    g_rxClockStart = frontTime - halfPeriodUs;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":238,"oldLines":10,"newStart":238,"newLines":12,"lines":[" ","     uint32_t frontTime = micros();"," ","-    // Hypothese de phase: le front capte est la frontiere de fin du demi-bit","-    // courant. On place donc g_rxClockStart pour que ce front tombe pile a la","-    // fin d\u0027un demi-bit (bitIndex=0, 2e demi-periode), et on relit les 8 bits.","-    g_rxClockStart = frontTime - halfPeriodUs;","+    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit","+    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc","+    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a","+    // frontTime -- son centre est une demi-periode plus tard, jamais dans le","+    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).","+    g_rxClockStart = frontTime;","     g_rxBitIndex = 0;"," ","     uint8_t preambleByte = 0;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01MGVXAUqPXEaUAL5YZVGoBo","duration_ms":16,"timestamp":"2026-07-03 23:45:50"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"ed6bade2-f5fe-432a-a306-cc11f8c8b704","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}","new_string":"  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}","newString":"  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":325,"oldLines":6,"newStart":325,"newLines":12,"lines":["   sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);"," ","   sendByte(END_BYTE);","+","+  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le","+  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse","+  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de","+  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.","+  digitalWrite(TX_PIN, LOW);"," }"," "," bool receiveFrame(Frame *f)"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01Di79Z9EpEL14QoSMfyWkcW","duration_ms":19,"timestamp":"2026-07-03 23:50:48"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"ed6bade2-f5fe-432a-a306-cc11f8c8b704","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }","new_string":"  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }","newString":"  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    bool idleLevel = digitalRead(RX_PIN) == HIGH;\n\n    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.\n    uint32_t armStart = micros();\n    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":219,"oldLines":11,"newStart":219,"newLines":18,"lines":[" ","   for (int attempt = 0; attempt \u003c maxAttempts; attempt++)","   {","-    bool idleLevel = digitalRead(RX_PIN) == HIGH;","+    // La ligne de repos est toujours LOW (sendFrame la force explicitement a","+    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant","+    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja","+    // en cours.","+    while (digitalRead(RX_PIN) == HIGH)","+    {","+      esp_rom_delay_us(5);","+    }"," ","-    // Attendre n\u0027importe quel front (transition de niveau) sur la ligne.","+    // Attendre le front montant LOW-\u003eHIGH.","     uint32_t armStart = micros();","-    while ((digitalRead(RX_PIN) == HIGH) == idleLevel)","+    while (digitalRead(RX_PIN) == LOW)","     {","       if ((micros() - armStart) \u003e 2000000UL)","       {"]},{"oldStart":231,"oldLines":7,"newStart":238,"newLines":7,"lines":["       }","       esp_rom_delay_us(5);","     }","-    if ((digitalRead(RX_PIN) == HIGH) == idleLevel)","+    if (digitalRead(RX_PIN) == LOW)","     {","       continue; // timeout d\u0027armement, on recommence","     }"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01E1hydWC6bSv7WNg1PQ8b2F","duration_ms":9,"timestamp":"2026-07-03 23:51:00"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"1c2b2931-f4f4-42c5-a990-b07cee20d0d0","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;","new_string":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.\n    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT\n    // ce front.\n    g_rxClockStart = frontTime - (halfPeriodUs / 2);\n    g_rxBitIndex = 0;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;","newString":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.\n    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT\n    // ce front.\n    g_rxClockStart = frontTime - (halfPeriodUs / 2);\n    g_rxBitIndex = 0;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit\n    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc\n    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a\n    // frontTime -- son centre est une demi-periode plus tard, jamais dans le\n    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).\n    g_rxClockStart = frontTime;\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":245,"oldLines":12,"newStart":245,"newLines":12,"lines":[" ","     uint32_t frontTime = micros();"," ","-    // Hypothese de phase: le front capte est la frontiere ENTRE le demi-bit","-    // qu\u0027on vient de quitter et le PROCHAIN demi-bit a echantillonner (donc","-    // le premier demi-bit du \"bit0\" qu\u0027on va lire commence exactement a","-    // frontTime -- son centre est une demi-periode plus tard, jamais dans le","-    // passe, pour que le busy-wait de receiveBitWithGlobalClock reste valide).","-    g_rxClockStart = frontTime;","+    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0","+    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas","+    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.","+    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT","+    // ce front.","+    g_rxClockStart = frontTime - (halfPeriodUs / 2);","     g_rxBitIndex = 0;"," ","     uint8_t preambleByte = 0;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01BwncryB5dWGEcbVHj7YTuP","duration_ms":15,"timestamp":"2026-07-03 23:57:02"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"1c2b2931-f4f4-42c5-a990-b07cee20d0d0","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.\n    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT\n    // ce front.\n    g_rxClockStart = frontTime - (halfPeriodUs / 2);\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }","new_string":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner\n    // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):\n    // son premier demi-bit (HIGH) commence exactement a frontTime.\n    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1\n\n    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;\n    for (int i = 0; i \u003c 7; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.\n    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT\n    // ce front.\n    g_rxClockStart = frontTime - (halfPeriodUs / 2);\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }","newString":"    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner\n    // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):\n    // son premier demi-bit (HIGH) commence exactement a frontTime.\n    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1\n\n    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;\n    for (int i = 0; i \u003c 7; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.\n    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT\n    // ce front.\n    g_rxClockStart = frontTime - (halfPeriodUs / 2);\n    g_rxBitIndex = 0;\n\n    uint8_t preambleByte = 0;\n    bool valid = true;\n    for (int i = 0; i \u003c 8; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":247,"oldLines":15,"newStart":247,"newLines":15,"lines":[" ","     // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0","     // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas","-    // a son debut). Le premier front montant capte apres l\u0027idle EST ce centre.","-    // g_rxClockStart (debut theorique du bit0) est donc une demi-periode AVANT","-    // ce front.","-    g_rxClockStart = frontTime - (halfPeriodUs / 2);","-    g_rxBitIndex = 0;","+    // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner","+    // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):","+    // son premier demi-bit (HIGH) commence exactement a frontTime.","+    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)","+    g_rxBitIndex = 1;                         // on commence la lecture au bit1"," ","-    uint8_t preambleByte = 0;","+    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)","     bool valid = true;","-    for (int i = 0; i \u003c 8; i++)","+    for (int i = 0; i \u003c 7; i++)","     {","       bool bit;","       if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_016WCyq6n5vA9nZY1UnX52JT","duration_ms":23,"timestamp":"2026-07-03 23:57:20"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"2f76b903-7b41-4cdb-9ea3-4d8877e888c8","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;","new_string":"    uint8_t preambleByte = 0; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;","newString":"    uint8_t preambleByte = 0; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner\n    // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):\n    // son premier demi-bit (HIGH) commence exactement a frontTime.\n    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1\n\n    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;\n    for (int i = 0; i \u003c 7; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":253,"oldLines":7,"newStart":253,"newLines":7,"lines":["     g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)","     g_rxBitIndex = 1;                         // on commence la lecture au bit1"," ","-    uint8_t preambleByte = 1; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)","+    uint8_t preambleByte = 0; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)","     bool valid = true;","     for (int i = 0; i \u003c 7; i++)","     {"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01CvgFdo1VR7HHWWKTQUGvjX","duration_ms":24,"timestamp":"2026-07-03 23:59:27"}
{"session_id":"0813f17e-1352-4a18-b545-7304c6ecab1b","transcript_path":"C:\\Users\\kgo20\\.claude\\projects\\c--Users-kgo20-Desktop---garder-UDS-S6-App4-tentative3\\0813f17e-1352-4a18-b545-7304c6ecab1b.jsonl","cwd":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3","prompt_id":"2f76b903-7b41-4cdb-9ea3-4d8877e888c8","permission_mode":"bypassPermissions","effort":{"level":"low"},"hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","old_string":"    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1","new_string":"    g_rxClockStart = frontTime - halfPeriodUs; // debut theorique du bit0 (le front est a son centre)\n    g_rxBitIndex = 1;                          // on commence la lecture au bit1","replace_all":false},"tool_response":{"filePath":"c:\\Users\\kgo20\\Desktop\\À garder\\UDS\\S6\\App4\\tentative3\\v1\\v1.ino","oldString":"    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1","newString":"    g_rxClockStart = frontTime - halfPeriodUs; // debut theorique du bit0 (le front est a son centre)\n    g_rxBitIndex = 1;                          // on commence la lecture au bit1","originalFile":"// ============================================================================\n// Communication full-duplex NRZ entre 2 ESP32 via bit-banging sur PIN 4/5\n// ============================================================================\n\n// Les includes Arduino et FreeRTOS sont déjà fournis par Arduino IDE\n#include \"esp_task_wdt.h\"\n#include \"rom/ets_sys.h\" // esp_rom_delay_us\n\n// CONFIGURATION GLOBALE\n#define ESP_ID 1 // 1 ou 2 -- à changer avant chaque upload\n\n#define TX_PIN 4\n#define RX_PIN 5\n#define BIT_PERIOD_US 1000 // duree totale d\u0027un bit Manchester (2 demi-periodes de 250us)\n\n#define PREAMBLE_BYTE 0x55 // 01010101 -- synchronisation\n#define START_BYTE 0x7E    // 01111110 -- délimiteur début\n#define END_BYTE 0x7E      // 01111110 -- délimiteur fin\n#define MAX_PAYLOAD 80\n\n#define FRAME_TYPE_DEBUT 0x01\n#define FRAME_TYPE_DATA 0x02\n#define FRAME_TYPE_FIN 0x03\n#define FRAME_TYPE_NACK 0x04\n\n#define BIT_ERROR_PROBABILITY_PCT 1 // 0 = jamais, 1 = force un flip sur le paquet seq=2 (test deterministe)\n\n// Structure de trame\ntypedef struct\n{\n  uint8_t type;\n  uint8_t seqNum;\n  uint8_t payloadLen;\n  uint8_t param;\n  uint8_t payload[MAX_PAYLOAD];\n  uint16_t crc;\n  bool crcValid; // rempli par receiveFrame(), pas transmis sur le fil\n} Frame;\n\n// Etat de sequencement/NACK (un seul producteur/consommateur chacun -\u003e volatile suffit)\nuint8_t g_rxExpectedSeq = 1;\nvolatile bool g_localGapDetected = false;\nvolatile uint8_t g_localGapTargetSeq = 0;\nvolatile bool g_remoteNackReceived = false;\nvolatile uint8_t g_remoteNackTargetSeq = 0;\n// Empeche de renvoyer un NACK a chaque nouvelle trame hors-sequence tant que\n// l\u0027episode d\u0027erreur en cours (meme trame attendue) n\u0027est pas resolu.\nbool g_nackAlreadySentForEpisode = false;\n\n// ============================================================================\n// LOGS DIFFÉRÉS\n// ============================================================================\n// Le RX verrouille son horloge d\u0027échantillonnage UNE SEULE FOIS sur le\n// préambule puis l\u0027extrapole pour TOUTE la trame. Le moindre appel bloquant\n// (Serial.print) pendant la réception/émission d\u0027une trame peut prendre\n// plusieurs ms et décale irrémédiablement le reste de la trame. Donc aucun\n// Serial.print ne doit avoir lieu pendant le bit-bang: on accumule le texte\n// dans un buffer global et on l\u0027affiche seulement une fois la trame terminée,\n// juste avant l\u0027attente entre deux envois/réceptions.\n\n#define LOG_BUF_SIZE 3072\nchar g_txLogBuf[LOG_BUF_SIZE];\nsize_t g_txLogLen = 0;\nchar g_rxLogBuf[LOG_BUF_SIZE];\nsize_t g_rxLogLen = 0;\n\nvoid logAppend(char *buf, size_t *len, const char *fmt, ...)\n{\n  if (*len \u003e= LOG_BUF_SIZE - 1)\n    return;\n  va_list args;\n  va_start(args, fmt);\n  int written = vsnprintf(buf + *len, LOG_BUF_SIZE - *len, fmt, args);\n  va_end(args);\n  if (written \u003e 0)\n    *len += written;\n}\n\nvoid logFlush(char *buf, size_t *len)\n{\n  if (*len \u003e 0)\n  {\n    Serial.print(buf);\n    Serial.println();\n    Serial.println();\n  }\n  buf[0] = \u0027\\0\u0027;\n  *len = 0;\n}\n\n// ============================================================================\n// ENCODAGE MANCHESTER BAS NIVEAU\n// ============================================================================\n// Convention (G.E. Thomas / IEEE 802.3): bit 1 = HIGH puis LOW (transition\n// descendante au centre du bit), bit 0 = LOW puis HIGH (transition montante\n// au centre du bit). BIT_PERIOD_US est la duree totale d\u0027UN bit, soit deux\n// demi-periodes de BIT_PERIOD_US/2 chacune.\n\n// Horloge TX absolue: chaque sendByte() repart d\u0027un temps de reference figé\n// une seule fois par trame (voir sendFrame), pour que digitalWrite()/l\u0027overhead\n// de boucle ne s\u0027accumule pas demi-bit apres demi-bit sur les trames longues\n// (payload jusqu\u0027a 80 octets = ~700 bits = ~1400 demi-bits). Un delai relatif\n// derive de quelques us par demi-bit, ce qui suffit a desynchroniser le\n// recepteur (horloge absolue, lui aussi figee une seule fois) sur une trame\n// de plusieurs centaines de bits.\nuint32_t g_txClockStart = 0;\nint g_txHalfBitIndex = 0;\n\nvoid sendHalfBit(bool levelHigh)\n{\n  digitalWrite(TX_PIN, levelHigh ? HIGH : LOW);\n  uint32_t targetTime = g_txClockStart + ((g_txHalfBitIndex + 1) * (uint32_t)(BIT_PERIOD_US / 2));\n  while (micros() \u003c targetTime)\n  {\n    // Spin jusqu\u0027a la fin exacte du demi-bit (horloge absolue, pas de derive cumulee)\n  }\n  g_txHalfBitIndex++;\n}\n\nvoid sendBit(bool bitValue)\n{\n  // bit 1: HIGH -\u003e LOW : bit 0: LOW -\u003e HIGH\n  sendHalfBit(bitValue ? HIGH : LOW);\n  sendHalfBit(bitValue ? LOW : HIGH);\n}\n\nvoid sendByte(uint8_t b)\n{\n  for (int i = 7; i \u003e= 0; i--)\n  {\n    sendBit((b \u003e\u003e i) \u0026 1);\n  }\n}\n\n// Variable globale pour synchroniser l\u0027horloge d\u0027échantillonnage (index de bit entier;\n// receiveBitWithGlobalClock derive en interne les deux demi-periodes correspondantes)\nuint32_t g_rxClockStart = 0;\nint g_rxBitIndex = 0;\n\n// Lit un bit Manchester en echantillonnant les deux demi-periodes: le niveau\n// du premier demi-bit donne directement la valeur (HIGH=1, LOW=0), le second\n// demi-bit est lu seulement pour verifier la transition attendue (integrite).\nbool receiveBitWithGlobalClock(uint32_t bitPeriodUs, int bitIndex, bool *outBit)\n{\n  if (!outBit)\n    return false;\n\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n  int firstHalfIndex = bitIndex * 2;\n\n  uint32_t sampleTime1 = g_rxClockStart + (firstHalfIndex * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t sampleTime2 = g_rxClockStart + ((firstHalfIndex + 1) * halfPeriodUs) + (halfPeriodUs / 2);\n  uint32_t timeoutTime = g_rxClockStart + ((firstHalfIndex + 2) * halfPeriodUs);\n\n  while (micros() \u003c sampleTime1)\n  {\n    // Spin jusqu\u0027au centre du premier demi-bit\n  }\n  bool firstHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c sampleTime2)\n  {\n    // Spin jusqu\u0027au centre du second demi-bit\n  }\n  bool secondHalf = digitalRead(RX_PIN) == HIGH;\n\n  while (micros() \u003c timeoutTime)\n  {\n    // Attendre la fin exacte du bit\n  }\n\n  if (firstHalf == secondHalf)\n  {\n    return false; // pas de transition au centre: viole le codage Manchester\n  }\n\n  *outBit = firstHalf; // HIGH-\u003eLOW = 1, LOW-\u003eHIGH = 0\n  return true;\n}\n\nbool receiveByte(uint8_t *outByte, uint32_t bitPeriodUs)\n{\n  if (!outByte)\n    return false;\n\n  uint8_t byte = 0;\n  for (int i = 0; i \u003c 8; i++)\n  {\n    bool bit;\n    if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n    {\n      return false;\n    }\n    byte = (byte \u003c\u003c 1) | (bit ? 1 : 0);\n    g_rxBitIndex++;\n  }\n  *outByte = byte;\n  return true;\n}\n\n// ============================================================================\n// CALIBRATION DU PRÉAMBULE (Manchester)\n// ============================================================================\n// Le préambule 0x55 (01010101) produit en Manchester une transition a chaque\n// demi-periode, sauf a une frontiere sur deux entre bits (plateau de 2 demi-\n// periodes au meme niveau) -- il y a donc plusieurs phases possibles pour le\n// premier front capte (il peut tomber sur n\u0027importe quelle frontiere de\n// demi-bit du preambule, pas seulement la toute premiere). On s\u0027accroche a un\n// front, on suppose une phase (le front = fin du demi-bit courant), puis on\n// relit les 8 bits pour valider 0x55. Si l\u0027hypothese est fausse, le busy-wait\n// de la relecture a deja consomme le temps reel correspondant: on ne peut pas\n// revenir en arriere, donc on rearme simplement sur le prochain front reel\n// (qui, statistiquement, finit par tomber sur la bonne phase en quelques\n// tentatives grace au bruit/gigue naturel de detection).\nbool detectAndCalibrateOnPreamble(uint32_t bitPeriodUs)\n{\n  const int maxAttempts = 60;\n  uint32_t halfPeriodUs = bitPeriodUs / 2;\n\n  for (int attempt = 0; attempt \u003c maxAttempts; attempt++)\n  {\n    // La ligne de repos est toujours LOW (sendFrame la force explicitement a\n    // LOW apres chaque trame). On s\u0027assure d\u0027abord d\u0027etre sur ce niveau avant\n    // d\u0027armer, pour ne pas s\u0027accrocher a un front au milieu d\u0027une trame deja\n    // en cours.\n    while (digitalRead(RX_PIN) == HIGH)\n    {\n      esp_rom_delay_us(5);\n    }\n\n    // Attendre le front montant LOW-\u003eHIGH.\n    uint32_t armStart = micros();\n    while (digitalRead(RX_PIN) == LOW)\n    {\n      if ((micros() - armStart) \u003e 2000000UL)\n      {\n        break; // rien reçu depuis 2s: on relance une nouvelle tentative propre\n      }\n      esp_rom_delay_us(5);\n    }\n    if (digitalRead(RX_PIN) == LOW)\n    {\n      continue; // timeout d\u0027armement, on recommence\n    }\n\n    uint32_t frontTime = micros();\n\n    // La ligne est idle LOW avant la trame. Le bit0 du preambule 0x55 vaut 0\n    // (LOW-\u003eHIGH), donc sa transition tombe exactement au CENTRE du bit0 (pas\n    // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner\n    // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):\n    // son premier demi-bit (HIGH) commence exactement a frontTime.\n    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)\n    g_rxBitIndex = 1;                         // on commence la lecture au bit1\n\n    uint8_t preambleByte = 0; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)\n    bool valid = true;\n    for (int i = 0; i \u003c 7; i++)\n    {\n      bool bit;\n      if (!receiveBitWithGlobalClock(bitPeriodUs, g_rxBitIndex, \u0026bit))\n      {\n        valid = false;\n        break;\n      }\n      preambleByte = (preambleByte \u003c\u003c 1) | (bit ? 1 : 0);\n      g_rxBitIndex++;\n    }\n\n    if (valid \u0026\u0026 preambleByte == PREAMBLE_BYTE)\n    {\n      // g_rxClockStart/g_rxBitIndex sont corrects: le prochain bit a lire\n      // est le bit8 (START_BYTE bit0).\n      return true;\n    }\n  }\n\n  return false;\n}\n\n// ============================================================================\n// GESTION TRAME COMPLÈTE\n// ============================================================================\n\nvoid buildFrame(Frame *f, uint8_t type, uint8_t seq, uint8_t param, const uint8_t *payload, uint8_t len)\n{\n  if (!f)\n    return;\n\n  f-\u003etype = type;\n  f-\u003eseqNum = seq;\n  f-\u003epayloadLen = (len \u003e MAX_PAYLOAD) ? MAX_PAYLOAD : len;\n  f-\u003eparam = param;\n  f-\u003ecrcValid = true;\n\n  if (payload \u0026\u0026 len \u003e 0)\n  {\n    memcpy(f-\u003epayload, payload, f-\u003epayloadLen);\n  }\n\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrc = computeCRC16(crcBuf, 4 + f-\u003epayloadLen);\n}\n\nvoid sendFrame(const Frame *f)\n{\n  if (!f)\n    return;\n\n  g_txClockStart = micros();\n  g_txHalfBitIndex = 0;\n\n  sendByte(PREAMBLE_BYTE);\n  sendByte(START_BYTE);\n\n  sendByte(f-\u003etype);\n  sendByte(f-\u003eseqNum);\n  sendByte(f-\u003epayloadLen);\n  sendByte(f-\u003eparam);\n\n  for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n  {\n    sendByte(f-\u003epayload[i]);\n  }\n\n  sendByte(f-\u003ecrc \u0026 0xFF);\n  sendByte((f-\u003ecrc \u003e\u003e 8) \u0026 0xFF);\n\n  sendByte(END_BYTE);\n\n  // Remet la ligne a un niveau de repos connu (LOW) apres chaque trame: le\n  // dernier demi-bit de END_BYTE (0x7E finit par un bit 0 = LOW-\u003eHIGH) laisse\n  // sinon la ligne a HIGH entre deux trames, ce qui ambiguise la detection de\n  // \"front\" au prochain armement de detectAndCalibrateOnPreamble.\n  digitalWrite(TX_PIN, LOW);\n}\n\nbool receiveFrame(Frame *f)\n{\n  if (!f)\n    return false;\n\n  g_rxClockStart = 0;\n  g_rxBitIndex = 0;\n\n  uint32_t bitPeriodUs = BIT_PERIOD_US;\n  if (!detectAndCalibrateOnPreamble(bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout detection preambule\\n\");\n    return false;\n  }\n\n  uint8_t startByte;\n  if (!receiveByte(\u0026startByte, bitPeriodUs) || startByte != START_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Start byte invalide (0x%02X)\\n\", startByte);\n    return false;\n  }\n\n  if (!receiveByte(\u0026f-\u003etype, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eseqNum, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003epayloadLen, bitPeriodUs) ||\n      !receiveByte(\u0026f-\u003eparam, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture entete\\n\");\n    return false;\n  }\n\n  if (f-\u003epayloadLen \u003e 0)\n  {\n    if (f-\u003epayloadLen \u003e MAX_PAYLOAD)\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Payload trop long\\n\");\n      return false;\n    }\n    for (int i = 0; i \u003c f-\u003epayloadLen; i++)\n    {\n      if (!receiveByte(\u0026f-\u003epayload[i], bitPeriodUs))\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture payload\\n\");\n        return false;\n      }\n    }\n  }\n\n  uint8_t crcLow, crcHigh;\n  if (!receiveByte(\u0026crcLow, bitPeriodUs) || !receiveByte(\u0026crcHigh, bitPeriodUs))\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: Timeout lecture CRC\\n\");\n    return false;\n  }\n  f-\u003ecrc = ((uint16_t)crcHigh \u003c\u003c 8) | crcLow;\n\n  uint8_t endByte;\n  if (!receiveByte(\u0026endByte, bitPeriodUs) || endByte != END_BYTE)\n  {\n    logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] ERREUR: End byte invalide (0x%02X)\\n\", endByte);\n    return false;\n  }\n\n  // Recalcul CRC apres capture complete de la trame (aucun bit-banging en cours ici)\n  uint8_t crcBuf[4 + MAX_PAYLOAD];\n  crcBuf[0] = f-\u003etype;\n  crcBuf[1] = f-\u003eseqNum;\n  crcBuf[2] = f-\u003epayloadLen;\n  crcBuf[3] = f-\u003eparam;\n  memcpy(\u0026crcBuf[4], f-\u003epayload, f-\u003epayloadLen);\n  f-\u003ecrcValid = (computeCRC16(crcBuf, 4 + f-\u003epayloadLen) == f-\u003ecrc);\n\n  return true;\n}\n\n// CRC-16-CCITT (poly 0x1021, init 0xFFFF)\nuint16_t computeCRC16(const uint8_t *data, uint16_t len)\n{\n  uint16_t crc = 0xFFFF;\n  for (uint16_t i = 0; i \u003c len; i++)\n  {\n    crc ^= ((uint16_t)data[i]) \u003c\u003c 8;\n    for (int b = 0; b \u003c 8; b++)\n    {\n      crc = (crc \u0026 0x8000) ? ((crc \u003c\u003c 1) ^ 0x1021) : (crc \u003c\u003c 1);\n    }\n  }\n  return crc;\n}\n\n// ============================================================================\n// ACQUISITION DE DONNEES SIMULEE\n// ============================================================================\n\n#define TOTAL_PAQUETS 5\n\nconst char *acquireDonnees(int index)\n{\n  static const char *donnees_txt[TOTAL_PAQUETS] = {\n      \"Ligne 1 - 2026-07-01 10:00:01 | Temp: 22.4 C | Humidite: 45.2 % | Node: 01\",\n      \"Ligne 2 - 2026-07-01 10:05:01 | Temp: 22.5 C | Humidite: 45.1 % | Node: 01\",\n      \"Ligne 3 - 2026-07-01 10:10:01 | Temp: 22.8 C | Humidite: 44.9 % | Node: 01\",\n      \"Ligne 4 - 2026-07-01 10:15:01 | Temp: 23.1 C | Humidite: 44.8 % | Node: 01\",\n      \"Ligne 5 - 2026-07-01 10:20:01 | Temp: 23.0 C | Humidite: 45.0 % | Node: 01\",\n  };\n  if (index \u003c 0 || index \u003e= TOTAL_PAQUETS)\n    return \"\";\n  return donnees_txt[index];\n}\n\nint loadDonneesIntoPackets(uint8_t packets[][MAX_PAYLOAD], uint8_t *packetLens, int maxPackets)\n{\n  int count = (TOTAL_PAQUETS \u003e maxPackets) ? maxPackets : TOTAL_PAQUETS;\n  for (int i = 0; i \u003c count; i++)\n  {\n    const char *line = acquireDonnees(i);\n    int len = strlen(line);\n    if (len \u003e MAX_PAYLOAD)\n      len = MAX_PAYLOAD;\n    memcpy(packets[i], line, len);\n    packetLens[i] = len;\n  }\n  return count;\n}\n\n// Injection d\u0027erreur deterministe pour test: BIT_ERROR_PROBABILITY_PCT=0 -\u003e jamais,\n// =1 -\u003e flip force sur le paquet seq=2 uniquement (reproductible pour debug/demo).\nbool g_errorAlreadyInjected = false; // n\u0027injecter qu\u0027une seule fois par session (pas a chaque retransmission)\n\nvoid maybeInjectBitError(Frame *f)\n{\n  if (!f || f-\u003epayloadLen == 0)\n    return;\n\n  if (BIT_ERROR_PROBABILITY_PCT != 0 \u0026\u0026 f-\u003eseqNum == 2 \u0026\u0026 !g_errorAlreadyInjected)\n  {\n    int byteIdx = 0;\n    int bitIdx = 3;\n    f-\u003epayload[byteIdx] ^= (1 \u003c\u003c bitIdx);\n    g_errorAlreadyInjected = true;\n    logAppend(g_txLogBuf, \u0026g_txLogLen,\n              \"[TX] *** ERREUR INJECTEE *** bit %d de l\u0027octet %d du paquet seq=%d\\n\",\n              bitIdx, byteIdx, f-\u003eseqNum);\n  }\n}\n\n// ============================================================================\n// LOGIQUE APPLICATIVE\n// ============================================================================\n\nvoid sendAcquisitionMessage()\n{\n  uint8_t packets[10][MAX_PAYLOAD];\n  uint8_t packetLens[10];\n  int totalPackets = loadDonneesIntoPackets(packets, packetLens, 10);\n\n  if (totalPackets == 0)\n    return;\n\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] J\u0027envoie %d paquets de donnees\\n\", totalPackets);\n  g_errorAlreadyInjected = false;\n  g_remoteNackReceived = false; // reset: ignorer tout residu d\u0027une session precedente\n  g_localGapDetected = false;\n\n  Frame fBegin;\n  buildFrame(\u0026fBegin, FRAME_TYPE_DEBUT, 0, totalPackets, 0, 0);\n  sendFrame(\u0026fBegin);\n\n  int i = 0;\n  while (i \u003c totalPackets)\n  {\n    if (g_remoteNackReceived)\n    {\n      uint8_t target = g_remoteNackTargetSeq;\n      g_remoteNackReceived = false;\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] NACK recu -\u003e retransmission a partir de la trame %d\\n\", target);\n      i = target - 1;\n      continue;\n    }\n\n    Frame fData;\n    buildFrame(\u0026fData, FRAME_TYPE_DATA, i + 1, 0, packets[i], packetLens[i]);\n    logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Envoi trame %d: %s\\n\", i + 1, acquireDonnees(i));\n    maybeInjectBitError(\u0026fData);\n    sendFrame(\u0026fData);\n\n    if (g_localGapDetected)\n    {\n      uint8_t target = g_localGapTargetSeq;\n      g_localGapDetected = false;\n      Frame fNack;\n      buildFrame(\u0026fNack, FRAME_TYPE_NACK, 0, target, 0, 0);\n      sendFrame(\u0026fNack);\n      logAppend(g_txLogBuf, \u0026g_txLogLen,\n                \"[TX] Erreur detectee sur la reception locale -\u003e envoi d\u0027un NACK pour la trame %d\\n\", target);\n    }\n\n    i++;\n  }\n\n  Frame fEnd;\n  buildFrame(\u0026fEnd, FRAME_TYPE_FIN, totalPackets, 0, 0, 0);\n  sendFrame(\u0026fEnd);\n  logAppend(g_txLogBuf, \u0026g_txLogLen, \"[TX] Session terminee (FIN envoye)\\n\");\n}\n\n// ============================================================================\n// TÂCHES FreeRTOS\n// ============================================================================\n\nvoid taskRX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(1500));\n\n  char assembledMsg[512] = {0};\n  int assembledLen = 0;\n\n  while (1)\n  {\n    Frame rxFrame;\n    memset(\u0026rxFrame, 0, sizeof(Frame));\n\n    if (!receiveFrame(\u0026rxFrame))\n    {\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n      continue;\n    }\n\n    if (rxFrame.type == FRAME_TYPE_DEBUT)\n    {\n      assembledLen = 0;\n      g_rxExpectedSeq = 1;\n      g_localGapDetected = false;\n      g_nackAlreadySentForEpisode = false;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Debut: %d paquets attendus\\n\", rxFrame.param);\n    }\n    else if (rxFrame.type == FRAME_TYPE_DATA)\n    {\n      if (!rxFrame.crcValid)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, erreur CRC detectee\\n\", rxFrame.seqNum);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (rxFrame.seqNum != g_rxExpectedSeq)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                  \"[RX] J\u0027ai recu trame %d: refusee, en attente de la trame %d\\n\",\n                  rxFrame.seqNum, g_rxExpectedSeq);\n        if (!g_nackAlreadySentForEpisode)\n        {\n          g_localGapTargetSeq = g_rxExpectedSeq;\n          g_localGapDetected = true;\n          g_nackAlreadySentForEpisode = true;\n        }\n      }\n      else if (assembledLen + rxFrame.payloadLen + 1 \u003c= (int)sizeof(assembledMsg) - 1)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] J\u0027ai recu trame %d: %.*s\\n\",\n                  rxFrame.seqNum, rxFrame.payloadLen, rxFrame.payload);\n        if (assembledLen \u003e 0)\n        {\n          assembledMsg[assembledLen] = \u0027\\n\u0027;\n          assembledLen++;\n        }\n        memcpy(\u0026assembledMsg[assembledLen], rxFrame.payload, rxFrame.payloadLen);\n        assembledLen += rxFrame.payloadLen;\n        g_rxExpectedSeq++;\n        g_nackAlreadySentForEpisode = false; // trame attendue recue: episode resolu\n      }\n    }\n    else if (rxFrame.type == FRAME_TYPE_FIN)\n    {\n      assembledMsg[assembledLen] = \u0027\\0\u0027;\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] Session terminee, message complet recu:\\n%s\\n\", assembledMsg);\n      memset(assembledMsg, 0, sizeof(assembledMsg));\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen); // fin de session: on affiche le resume complet\n    }\n    else if (rxFrame.type == FRAME_TYPE_NACK)\n    {\n      if (!g_remoteNackReceived)\n      {\n        logAppend(g_rxLogBuf, \u0026g_rxLogLen, \"[RX] NACK recu pour paquet %d\\n\", rxFrame.param);\n        g_remoteNackTargetSeq = rxFrame.param;\n        g_remoteNackReceived = true;\n      }\n    }\n    else\n    {\n      logAppend(g_rxLogBuf, \u0026g_rxLogLen,\n                \"[RX] ERREUR: type de trame inconnu 0x%02X seq=%d len=%d param=%d\\n\",\n                rxFrame.type, rxFrame.seqNum, rxFrame.payloadLen, rxFrame.param);\n      logFlush(g_rxLogBuf, \u0026g_rxLogLen);\n    }\n  }\n}\n\nvoid taskTX(void *pvParameters)\n{\n  vTaskDelay(pdMS_TO_TICKS(3000));\n\n  while (1)\n  {\n    sendAcquisitionMessage();\n    logFlush(g_txLogBuf, \u0026g_txLogLen);\n    vTaskDelay(pdMS_TO_TICKS(5000));\n  }\n}\n\n// ============================================================================\n// SETUP / LOOP\n// ============================================================================\n\nvoid setup()\n{\n  Serial.begin(115200);\n  delay(500);\n  randomSeed(esp_random());\n\n  // Le RX doit boucler en busy-wait (précision microseconde) sans jamais\n  // rendre la main pendant toute la durée d\u0027une trame (~700ms pour un message\n  // complet), ce qui empêche la tâche IDLE du coeur 0 de tourner et déclenche\n  // le Task Watchdog (reboot en boucle). On désactive donc entièrement le\n  // TWDT: c\u0027est un choix assumé pour ce protocole bit-bang temps réel strict,\n  // pas un correctif de contournement d\u0027un autre bug.\n  esp_task_wdt_deinit();\n\n  pinMode(TX_PIN, OUTPUT);\n  pinMode(RX_PIN, INPUT);\n  digitalWrite(TX_PIN, LOW);\n\n  xTaskCreatePinnedToCore(taskRX, \"TaskRX\", 4096, 0, 1, 0, 0);\n  xTaskCreatePinnedToCore(taskTX, \"TaskTX\", 4096, 0, 1, 0, 1);\n}\n\nvoid loop()\n{\n  vTaskDelay(pdMS_TO_TICKS(1000));\n}\n","structuredPatch":[{"oldStart":250,"oldLines":8,"newStart":250,"newLines":8,"lines":["     // a son debut) -- ce centre vient de passer, on ne peut plus l\u0027echantillonner","     // retroactivement. On se positionne donc sur le bit1 (valeur 1, HIGH-\u003eLOW):","     // son premier demi-bit (HIGH) commence exactement a frontTime.","-    g_rxClockStart = frontTime - bitPeriodUs; // debut theorique du bit0 (dans le passe, jamais echantillonne directement)","-    g_rxBitIndex = 1;                         // on commence la lecture au bit1","+    g_rxClockStart = frontTime - halfPeriodUs; // debut theorique du bit0 (le front est a son centre)","+    g_rxBitIndex = 1;                          // on commence la lecture au bit1"," ","     uint8_t preambleByte = 0; // bit0 du preambule 0x55 est toujours 0 (implicite: front LOW-\u003eHIGH capte)","     bool valid = true;"]}],"userModified":false,"replaceAll":false},"tool_use_id":"toolu_01Jh7bRWiWQSH3R2Xga3AMa9","duration_ms":28,"timestamp":"2026-07-04 00:00:04"}
